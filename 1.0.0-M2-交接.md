# Jovan's Workplace · 1.0.0 M2 交接文档（工具循环 + 一句话解析 + 全模块主动检测 + 思考滑块）

> 对应开工对接文档 `1.0.0-M2-开工对接.md`。M2 五步已全部完成并通过真实 API 实测。
> 交接日期：2026-09-01 · 执行模型：DeepSeek V4 Pro（WorkBuddy）

---

## 一、完成情况总览

| 步骤 | 内容 | 状态 |
|---|---|---|
| Step 1 | 工具循环引擎 `runAgentLoop` | ✅ 完成 + 端到端实测 |
| Step 2 | 工作台工具集（5 读 + 5 写草稿） | ✅ 完成 + 单元验证 |
| Step 3 | 一句话解析迁移（弃 8787 桥）+ 学习词典迁移 | ✅ 完成 + 端到端实测 |
| Step 4 | F1 全模块主动检测（8 类意图） | ✅ 完成 + 端到端实测 |
| Step 5 | 思考深度滑块（参数实测 → UI → 费用联动） | ✅ 完成 + 参数实测 |

---

## 二、Step 5 思考参数实测结果（重点，实测于 2026-09-01）

实测对象：`deepseek-v4-flash`（DeepSeek V4），直连 `https://api.deepseek.com/v1`。

### 1. 思考开关 `thinking`

| 请求 | HTTP | reasoning_content |
|---|---|---|
| `thinking: { type: 'disabled' }` | 200 | **无**（关闭生效） |
| `thinking: { type: 'enabled' }` | 200 | 有 |

### 2. 思考强度 `reasoning_effort`

| 值 | HTTP | 结果 |
|---|---|---|
| `low` | 200 | 接受，有 reasoning |
| `high` | 200 | 接受，有 reasoning |
| `max` | 200 | 接受，有 reasoning |

> 官方枚举为 `low / medium / high / xhigh / max`。实测确认 `low / high / max` 均被接受。

### 3. ai.js 的滑块 → 参数映射（`THINKING_LEVELS`）

| UI 档位 | thinking | reasoning_effort |
|---|---|---|
| 关闭 | `disabled` | — |
| 浅 | `enabled` | `low` |
| 标准 | `enabled` | `high` |
| 深 | `enabled` | `max` |

### 4. 工具循环中 `reasoning_content` 回传（关键坑的实测结论）

- **之前预判**：带 `tools` 的请求，assistant 的 `reasoning_content` 必须回传，否则 HTTP 400。
- **实测结论**：二轮回传 reasoning → 200；**不回传 reasoning → 也 200**。DeepSeek V4 当前**不强制**回传。
- **处理**：`runAgentLoop` 仍保留回传逻辑（`messages.push` 时带上 `reasoning_content`），无害且对可能的模型差异更稳妥。

### 5. 费用联动实测

- 工具循环 2 轮（问时间 → 调 `get_time` → 回传 → 最终答案），usage 跨轮累计：input 784 + output 81 = **865 token，¥0.0015**。
- 单轮无思考：10 + 1 = 11 token，¥0.0000195。
- 结论：`runAgentLoop` 跨轮正确累计 usage 并折算 RMB，UI 费用联动数据源正确。

---

## 三、工具 schema 清单（`tools.js`，共 10 个）

### 读工具（直接执行，返回结果）

| 工具 | 参数 | 说明 |
|---|---|---|
| `query_today` | 无 | 今日待办 / 逾期 / 近 7 天生日概览 |
| `query_schedules` | keyword, date | 查日程+任务（日期/关键字） |
| `query_memos` | keyword | 查备忘 |
| `query_family` | keyword | 查亲友画像（忌口/雷点/喜好） |
| `query_study` | keyword | 查学习/备考（学期/科目/标签） |

### 写工具（返回草稿，用户确认后才 `data.add`）

| 工具 | 必填 | 可选 | 说明 |
|---|---|---|---|
| `create_schedule` | 标题 | 日期/时间/地点/优先级/详情 | 日程草稿 |
| `create_task` | 标题 | 优先级/日期/标签/详情 | 任务草稿 |
| `create_memo` | 标题 | 详情 | 备忘草稿（日期=今天） |
| `create_family` | 姓名 | 关系/忌口/雷点/喜好/生日 | 亲友画像草稿 |
| `create_study` | 标题或科目 | 学期/科目/备考 | 学习记录（备考归入「备考」类型） |

**安全底线**：写工具一律 `{draft:true, ...}` 返回，经 `ai:agent-event` 推 UI 弹确认框，用户点确认后走 `ai:confirm-draft` → `data.add`。绝不自动写库。

**draft 数据流（易误解，务必注意）**：写工具内部 `draftEvent(onEvent, draft)` 把**含 `props` 的 draft 对象**通过 `onEvent({type:'draft', draft})` 推给 renderer；而 `executeTool` 的**返回值**是 `{draft:true, 已生成草稿:'预览字符串'}`（给模型回填看，**不含 props**）。所以 `props` 只能从 onEvent 事件取，不能从返回值取。

**归一化函数**（`tools.js` 导出）：`normalizeDate`（今天/明天/后天/周X/下周X/下下周X/M月D日/YYYY-MM-DD）、`normPriority`（PRI_ALIAS 映射到四象限）、`normType`（类型白名单）。

---

## 四、学习词典迁移

- 来源：renderer localStorage `wb_parse_dict`
- 目标：`data\dict.json`（主进程读写，跨设备同步）
- 实现：`parse.js` 的 `dictLoad / dictSave / dictClear` + IPC `ai:dict-load / ai:dict-save / ai:dict-clear`
- renderer：`parseDictGet/Save` 改为内存 `DICT_CACHE` + IPC 同步，初始化时 `loadDictFromMain()`

---

## 五、遇到的坑（务必留意）

1. **safeStorage DPAPI 绑定 app name**：直接 `electron xxx.js` 跑脚本时 app name 是默认 `"Electron"`，`safeStorage.decryptString` 会解出乱码 key 导致 401（或直接抛 "Error while decrypting"）。**解法**：脚本顶部 `app.setName("Jovan's Workplace")` + `app.setPath('userData', "C:\\Users\\17169\\AppData\\Roaming\\Jovan's Workplace")`，且在 `app.whenReady()` 之前。
2. **沙盒 GPU 崩溃**：`app.disableHardwareAcceleration()` 或 `--disable-gpu` 单独不够，GPU 进程在 fetch 等待期间崩溃触发 `FATAL: GPU process isn't usable. Goodbye.`（EXIT=3）。**解法**：命令行加 `--no-sandbox --disable-gpu --disable-gpu-compositing`。真实桌面双击运行无此问题。
3. **DeepSeek key 曾失效（401）**：旧 key `sk-...0b2c` 已失效，已用新 key 重新写入 `ai-keys.json`（safeStorage 加密）并验证连通。
4. **相对日期解析失败**：模型不知道"今天"，`明天/周五` 会留空。**解法**：`parse.js` 新增 `todayCN()` 把当天日期+星期注入 system prompt，实测「明天→2026-09-02」「周五→2026-09-04」正确。
5. **tools.js 嵌套括号语法错误**（开发期）：`TOOL_DEFS` 数组字面量嵌套过深导致 `Unexpected token '{'`，已改用 `fn(name, description, properties, required)` 辅助函数构建。
6. **写工具 draft 数据流易误解**（集成测试期）：写工具的 `props` 只能从 `onEvent({type:'draft'})` 事件取，`executeTool` 返回值不含 `props`（见「三」安全底线说明）。集成测试曾误从返回值取 `.props` 得到 undefined。

---

## 六、代码改动清单

| 文件 | 状态 | 改动 |
|---|---|---|
| `app/ai.js` | 修改 | +`chatOnce`（非流式）、+`jsonOutput`（JSON mode + 校验重试）、+`runAgentLoop`（工具循环）、+`thinkingParams`/`THINKING_LEVELS`、+`maxToolRounds`/`readAgentCfg`、`chat()` 注入 thinking |
| `app/tools.js` | **新增** | 10 工具定义 + `executeTool` 分发 + 归一化函数 |
| `app/parse.js` | **新增** | `parse`（一句话解析）+ `detect`（8 类意图）+ 学习词典 + `todayCN` |
| `app/main.js` | 修改 | 引入 tools/parse；+IPC `ai:agent`/`ai:parse`/`ai:detect`/`ai:dict-*`/`ai:confirm-draft` |
| `app/preload.js` | 修改 | 暴露 `aiAgent`（带事件）/`aiParse`/`aiDetect`/`aiDictLoad/Save/Clear`/`aiConfirmDraft` |
| `app/renderer/index.html` | 修改 | 思考滑块 UI + 步骤条 + draft/notice 确认卡片 + 解析/检测接入 + 删除 8787 BRIDGE |

---

## 七、验收标准对照

| # | 验收项 | 结果 |
|---|---|---|
| 1 | 一句话「周五交计网作业，紧急」→ {任务, 标题, 日期:本周五, 优先级} → 写入 | ✅ 实测日期=2026-09-04、优先级=紧急且重要；写入→读取链路实测闭环 |
| 2 | 对话「下周约导师」→ 日程确认框 → 写入 | ✅ detect 生成日程 draft + 确认框 |
| 3 | 对话「别忘了我妈不吃香菜」→ 备忘确认框 → 写入 | ✅ detect + create_memo |
| 4 | 工具循环复合问题 → 步骤条 + 收敛 + 不死循环 | ✅ PROBE 实测 2 轮收敛 |
| 5 | 滑块 → 参数变化 → 费用联动 | ✅ off/shallow/deep 实测 + usage 累计 |
| 6 | 手动添加等旧功能回归 | ✅ 冒烟启动无错误 + 手动 `data.add` 集成测试通过 |
| 7 | 交接文档交付 | ✅ 本文档 |

---

## 八、已知限制 / 后续改进点

1. **相对日期依赖模型换算**：已通过 `todayCN()` 注入缓解，但跨月/跨年边界（如"下周三"恰在月末）仍可能有误差，建议后续加本地兜底校验（`normalizeDate` 已支持自然语言，可在模型输出留空时二次解析）。
2. **低置信度未接「AI 深度解析（pro）」**：`parse` 已返回 `confidence`，但 UI 尚未实现低置信度时提示切换 pro 模型的入口。
3. **detect 的冲突/逾期检测**：当前依赖模型对"最近对话"的理解，未做本地数据比对（撞日程/逾期），后续可在 `detect` 前注入 `query_today` 结果辅助判断。
4. **主动检测触发时机**：当前在对话后触发，未做防抖/频率控制，连续多轮可能重复弹框，后续可加"本轮已确认则不重复"去重。

---

## 九、实测复现命令（供后续回归）

```bash
# 注意：Electron 脚本需先 set 正确 app name + userData（见「坑 1」），
# 并加 --no-sandbox --disable-gpu --disable-gpu-compositing（见「坑 2」）。
cd "/d/Jovan's Workplace/app"

# 思考参数 + 工具循环（Electron 版）
env -u ELECTRON_RUN_AS_NODE NODE_OPTIONS= ./node_modules/.bin/electron test-thinking.js \
  --no-sandbox --disable-gpu --disable-gpu-compositing

# parse / detect（Electron 版）
env -u ELECTRON_RUN_AS_NODE NODE_OPTIONS= ./node_modules/.bin/electron test-parse.js \
  --no-sandbox --disable-gpu --disable-gpu-compositing
```

> 实测脚本为临时文件，验证后已删除；如需回归可参照本文件「二」「三」章节临时重建。

---

*交接完成 · 2026-09-01*
