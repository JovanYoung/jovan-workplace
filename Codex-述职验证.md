# Codex 述职验证包（喂给 Codex 的记忆 + 考题）

> 用途：Codex 建项目后先"读记忆 + 述职"，证明它真正理解 JW 的蓝图、历史错误与埋坑，用户据此判断是否放行。
> 日期：2026-09-06 · 配套必读：`Codex-项目交接.md`（主记忆）、`AI功能蓝图.md`、`开工把关清单.md`

---

## 一、喂给 Codex 的启动指令（用户直接复制）

```
你在 Jovan's Workplace（JW）项目担任执行工程师。请先完成"记忆加载 + 述职"，
通过考核后才能接开发任务。

【第一步 · 读记忆】依次读完以下文件（都在 D:\Jovan's Workplace\ 下）：
1. Codex-项目交接.md（项目全记忆：架构/里程碑/约定/环境坑）
2. AI功能蓝图.md（需求总账与版本节奏）
3. 1.3-更新交接.md（下一期需求）
4. 1.2-Agent编排升级-交接文档.md + 1.2-Agent编排升级-危险操作报告.md（上一期执行记录）
5. dsh-借鉴清单.md（借鉴决策）
6. 开工把关清单.md（流程规范）

【第二步 · 述职】按 Codex-述职验证.md 的考题逐条回答，格式：
- ✅ 我知道：[用自己的话复述要点]（出处：文件名/提交号）
- ⚠️ 有出入：[你认为正确的内容]，依据是…
- ❌ 我不知道：老实说不确定
全程不得编造；不确定就说不确定并指出该去读哪份文件。

【第三步 · 汇报】述职完成后给用户一句话总结：你理解最深的 3 件事 + 你认为最危险/最该小心的 3 个坑。
```

## 二、述职考题

### A 组 · 蓝图与方案（12 题）

1. JW 的产品定位从什么演进到什么？发生在哪个版本（关键提交）？主页布局现在是什么样、1.3 要回退什么？
2. 为什么坚持 Electron + 纯 JS、不换 Rust/不换开源底座？dsh 架构拆解结论里，我们"抄设计不抄代码"的 4 个模式是什么？
3. 记忆四层（L1-L4）各是什么？L3 为什么从 Mem0 改为自建？Hermes 和 DeepTutor 各借鉴了哪一条核心设计？
4. 工具循环的"写工具草稿制"解决什么问题？draft 的 props 从哪个通道取（为什么不能从工具返回值取）？
5. 前缀缓存友好布局是什么？fixedSystem 与 dynamicInjection 各指什么、为什么这么分？
6. 周期日程（recurrence）设计定稿：存规则不展开成多条——为什么？视图展开、编辑"仅此一次/所有重复"指什么？
7. 会话体系：主 Agent 与学科会话怎么区分存储？历史查看器按什么分组？主 Agent 按天落库从哪个版本开始？
8. 1.3 全部 8 项需求列一遍；每项拍板默认值是什么？
9. i18n 三方向（A/B/C）与 UI 优化三方向（A/B/C）是什么？推荐组合与理由？
10. 子 Agent"灵魂碎片"设计：继承什么、归档回哪里、打开样式参照谁？
11. 产品里有哪些"借鉴清单"文档？dsh/Hermes/DeepTutor 各自的代表作借鉴点各一条？
12. 版本时间线：0.0.0 → M1-M4 → 1.0.0 → 1.1 → 1.2 各自的关键提交号与一句话内容。

### B 组 · 历史错误与踩坑（11 题）

1. WorkBuddy/桌面环境注入的三个坑是什么（NODE_OPTIONS、ELECTRON_RUN_AS_NODE、GPU）？各怎么绕？
2. 带 BrowserWindow 的冒烟脚本有什么怪现象？正确做法？
3. safeStorage 解出乱码 key 的根因与修复？
4. renderer 为什么禁 window.prompt/confirm？当前代码里还有哪些历史遗留用它们？
5. SQLite 中文检索的坑：unicode61/trigram 各自问题？最终方案？
6. draft 确认卡集成时踩过什么 props 通道的错（M2 坑 6）？
7. 步骤条出过哪两个 bug（M2）？怎么修的？
8. 学科 AI 对话曾经缺什么导致"日程写不进"（用户实测发现的）？
9. DeepSeek API key 一共失效过几次、尾号分别是什么？
10. 执行期 Edit 纪律坑（并发修改/HTML 闭合/函数头误删）怎么避免？
11. renderShortcutPanel 的隐藏 bug 是什么？

### C 组 · 埋下的坑与待办（15 题）

1. 打字机效果是模拟还是真流式？真流式为什么没做、排在哪？
2. ask/plan 桥接的"单 resolver"假设是什么？并发会怎样？
3. fallback 模型切换的边界（重试几次？切到哪？全没配置时？）
4. shouldPlan 是纯关键词启发式——误触发/漏触发怎么调？
5. deleteConversation 的删除语义：会话删了 facts 会怎样？这个决策定了吗？
6. renderer 里还有哪些 8787 桥残留（属于哪个模块的未来重构）？
7. 网页版（workspace-build）与桌面版的分叉状态是什么？
8. 1.2 的 6 项 GUI 待确认是什么？（用户可能还没复验）
9. 下载器端到端实测过吗？1.0.0 为什么还没打包发行？（SHA256 联动链路是什么？）
10. 发行时对用户的提醒项（SmartScreen/杀软）？
11. 语音输入为什么做不了？方案选项是什么？
12. 学习词典与技能库的"两级沉淀"分工？hits 计数接好线了吗？
13. backlog 排队清单（MCP/网页搜索/定时/周期/真流式/subagent/语音）各自的触发条件？
14. 备忘模块改名机制（常量名）？现在占位名是什么？
15. 1.3 全量 8 需求在 Codex Plus 额度下要注意什么（5h 窗口/模型分工）？

### 述职格式要求

- 每一条：✅/⚠️/❌ + 要点（≤3 行）+ 出处（文件名/提交号，找不到就写"文档未覆盖，我推断…"）
- 全部答完再给"三最"总结（最懂 3 条 / 最危险 3 条）
- 严禁编造提交号或文件内容

## 三、用户判断标准

- **放行**：A 组 ≥10 题 ✅ 且能说出关键出处；B 组 ≥8 题 ✅；C 组能列全 backlog 且指出至少 5 个自己会小心的坑
- **再读一轮**：A 组 <8 或 B/C 大量 ⚠️/❌
- 注意：⚠️ 有出入不一定是它错——拿文档核对，它若指出文档过时/错误反而加分
