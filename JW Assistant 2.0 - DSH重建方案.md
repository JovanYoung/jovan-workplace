# JW Assistant 2.0：以 DeepSeek Harness 为内核的重建方案

> 状态：**产品重定向草案，尚未安装 DSH、插件或迁移任何数据。**
>
> 决策背景：JW 不再以“个人效率工作台”为核心，而定位为一个 Siri 式、可长期成长的个人助理。

---

## 1. 一句话定义

> **JW Assistant 是一个理解你的上下文、能安全完成任务、会在确认后成长的个人助理。**

用户应该先说自己的意图，而不是先进入“日程”“学习”“文件”页面。例如：

- “明天下午三点提醒我交 CSC 作业。”
- “把这节课总结进 GSS1，再给我三道复习题。”
- “查这篇论文的作者和主要结论。”
- “这周有哪些快到期的事情？”

学习、日程、资料和浏览器都是助理调用的能力页；不再是逼用户先选择的工作台入口。

## 2. 关键架构决定

### 2.1 DSH 是内核，JW 是发行版

这不是“Electron JW 调一个 DSH 服务”，也不是“给旧 JW 接更多 Agent 功能”。

```text
JW Assistant（独立 DSH Profile / Bundle）
├─ DSH 内核：会话、Agent loop、工具、子 Agent、模型、MCP、审批、调度
├─ JW 助理 UI：配色、语音/文字入口、行动中心、能力页
├─ JW 能力插件：学习 / 日程 / 文件 / 生活 / 记忆
├─ JW 安全策略：风险分级、确认、外发限制、审计回执
└─ JW 数据层：可迁移、可导出、可回滚
```

DSH 的会话、工具、Agent loop、模型适配器和 Web Client 都是插件化组成，profile/bundle 是其原生扩展方式。因此 JW 应该在 DSH 的 `web` profile 上组合自己的 bundle，而不修改 DSH core。

**现有 Electron JW 的新定位：**数据语义参考、交互原型、迁移来源与回退档案；不再继续承担下一代 Agent 内核。

### 2.2 为什么不是继续改旧 JW

旧 JW 已手写了聊天、模型路由、会话、分支、提醒、学习上下文和确认卡。继续走下去会不断重造 DSH 已解决的共性基础设施，并让学习小 Agent、MCP、工具审计和长期任务各自长出不兼容的实现。

DSH 的价值不是“多一个聊天界面”，而是让上述能力共享同一份运行时、会话轨迹、权限与工具调度。

## 3. 产品交互：助理首页，而非仪表盘

首页只保留四件事：

1. **对话与语音入口**：用户可直接说目标。
2. **进行中的行动**：现在在做什么、已经完成什么、是否等待用户。
3. **少量今日状态**：真正需要注意的提醒，而非完整仪表盘。
4. **行动中心**：所有待批准、可撤销、失败重试的动作。

核心闭环：

```text
用户意图
→ 助理复述目标与必要假设
→ 读取记忆 / 资料 / 工具
→ 输出计划或执行只读动作
→ 高风险节点请求确认
→ 返回结果、来源、变更和回执
→ 询问是否沉淀为记忆或技能候选
```

## 4. Hermes 借鉴：成长机制，不是运行底座

Hermes 的启发是闭环学习：长期记忆、跨会话检索、从复杂任务生成可复用技能、让技能在使用中改进。

JW 应借鉴的具体行为：

- 每个课程可拥有课程上下文、资料、错题、复习历史与专属技能；
- 复杂任务结束后生成“技能候选”，例如“按某老师 rubric 检查作业”；
- 用户可批准、编辑、禁用、导出和回滚技能；
- 每周生成学习回顾草稿，而非后台擅自执行外部动作；
- 课程 Agent 是同一主助理在不同课程上下文下运行，不复制底层工具和权限。

**不照搬：**无确认的自我修改、无限制常驻任务、多平台外发和黑箱记忆写入。

## 5. 数据、记忆与技能必须分层

| 层 | 保存内容 | 写入规则 |
|---|---|---|
| 原始事实 | 日程、任务、笔记、资料索引 | 结构化写入，保留来源与修改记录 |
| 会话轨迹 | 对话、工具调用、执行进度 | 可搜索、可归档，不自动成为长期记忆 |
| 长期记忆 | 偏好、课程背景、稳定习惯 | 先成为候选；经确认或低风险规则后写入 |
| 技能 | 可复用工作流、课程方法、检查表 | 版本化、可编辑、可禁用、可导出 |
| 摘要 | 上下文压缩 | 可再生成，绝不覆盖原文或原始事实 |

硬规则：课程记忆默认不跨课程泄漏；原始笔记优先于模型总结；每条长期记忆必须可追溯、可纠正、可删除。

## 6. 统一安全与审批模型

| 级别 | 示例 | 默认策略 |
|---|---|---|
| R0 只读 | 查日程、读已授权资料、公开网页搜索 | 自动执行，展示来源 |
| R1 JW 内可逆写入 | 待办草稿、笔记草稿、技能候选 | 展示变更预览后确认 |
| R2 外部或难逆操作 | 发消息、上传文件、提交网页表单、改重要日程 | 每次明确确认 |
| R3 高危 | 删除、覆盖、凭据、系统路径、批量操作、远程命令 | 永不自动批准，逐项确认 |

`dsh-approval-gate` 可承担审批交互与动作分流，但不能让模型取代用户对 R2/R3 的确认。原则是 **fail closed**：系统无法可靠判定风险时，默认不执行。

## 7. 已发现插件：采用顺序与边界

所有下列均是社区插件或社区目录条目，不构成安全背书。安装前必须审查源码、依赖、构建脚本、权限与固定版本/commit；不得使用 `latest`。

| 插件 | 借鉴/用途 | 建议阶段 | 约束 |
|---|---|---:|---|
| `dsh-web-ui-all` | UI 扩展、皮肤、状态/任务可视化 | 1 后 | 只挑 JW 需要的 UI 能力；不一键开放远控、SSH、危险 Git 操作 |
| `dsh-approval-gate` | 行动审批、风险提示 | 1 | 必须落实本方案 R0–R3；R2/R3 不自动放行 |
| `modsearch` | 结构化联网检索与原生引用展示 | 2 | 搜索词属于外发；验证来源质量、失败回退和引用真实性 |
| `dsh-ego-browser` | 可观察浏览器 Agent、人工接管、站点动作记忆 | 4 | 默认只读；登录、上传、下载、提交、支付升级 R2/R3 |

尤其注意：`dsh-web-ui-all` 是功能集合，不等于 JW 的基础 UI；先跑干净的 DSH，再按白名单逐一安装、回归和记录。`dsh-ego-browser` 的“人工接管”很适合个人助理，但绝不能让浏览器插件绕过统一审批层。

## 8. 分阶段路线

### 阶段 0：跑通并冻结 DSH 基线

**目标：**证明 DSH 能稳定作为 JW 内核；不迁移旧数据。

交付：

- 单独的 `jw-assistant` profile、开发 profile 与空白测试 profile；
- 固定 DSH、Node、所有 plugin 的版本和 lockfile；
- 会话、工具、模型、审批拒绝/通过、重启恢复的最小闭环；
- 插件审计清单与安装记录；
- 配置和测试轨迹的一键导出。

验收：断网、模型失败、插件失效时不会损坏 profile；不使用自动升级或 `latest`；R2/R3 测试动作无法绕过确认。

### 阶段 1：JW 助理壳与行动中心

**目标：**先让它像 JW 助理，再扩展能力。

交付：JW 主题、对话/语音入口、执行进度、行动中心、基础记忆候选可视化与统一审批层。

验收：用户可从首页完成“询问 → 计划 → 审批 → 回执”；首页不退化为旧仪表盘；所有写入均可说明变更。

### 阶段 2：迁移 JW 的核心价值

顺序：

1. 日程/任务与 recurrence 规则；
2. 学习课程、笔记、资料索引与课程上下文；
3. 旧 JW 的只读导入器：映射、预览、冲突报告、导入回执；
4. `modsearch` 与来源引用。

验收：旧数据迁移前后可按条目数、标识、时间、内容和周期规则核对；recurrence 仍只存规则，绝不展开为大量持久条目。

### 阶段 3：助理成长与课程 Agent

交付：课程上下文、课堂字幕/转写能力、课后笔记与复习题草稿、技能候选、周回顾草稿、受控子 Agent。

验收：课程之间不串上下文；技能可批准/编辑/禁用/回滚；主动提醒不造成未经许可的外发或写入。

### 阶段 4：受控浏览器与外部世界

交付：`dsh-ego-browser` 只读研究模式、人工接管、站点权限、页面操作记录、受控文件工作流。

验收：登录、上传、下载、表单提交、支付均被拦截并等待确认；用户可看见浏览轨迹和最终提交内容。

## 9. 旧 JW 的迁移纪律

```text
旧 JW（冻结为迁移来源）
→ 只读扫描
→ 映射与冲突报告
→ 用户预览确认
→ 写入新数据层
→ 核验、回执
→ 保留旧数据与只读回退
```

- 不直接修改旧 `data/`；
- 每种实体使用独立迁移器，先 dry-run 后导入；
- 保留原始 ID、创建时间、内容、周期规则和来源；
- 新旧并行核验后，旧 Electron JW 才降级为只读历史查看器；
- 不删除旧应用，直到迁移已被用户确认。

## 10. 首批验收指标

| 维度 | 成功标准 |
|---|---|
| 助理体验 | 常见请求能从首页完成，无需先找模块 |
| 安全 | R2/R3 零自动放行；每项写操作有审批和回执 |
| 可恢复 | 重启或模型失败后，任务状态一致且不重复执行 |
| 数据完整 | 迁移抽样 100% 可核对；周期规则未展开 |
| 记忆质量 | 记忆可追溯、编辑、删除；课程间默认隔离 |
| 课程价值 | 能调用对应课程资料并产出可确认的笔记/复习成果 |
| 可维护 | 升级只适配 JW profile/bundle，不修改 DSH core |

## 11. 需要用户确认的产品决策

1. 第一版只服务你个人，还是预留给同学使用？
2. 首页采用纯对话，还是“对话 + 极简今日状态”？
3. 语音首版接受本机/浏览器能力，还是必须接云端 ASR？
4. 迁移后是否保留旧 Electron JW 作为只读档案？
5. 长期记忆是否一律候选待确认，还是允许低风险偏好自动写入？
6. 浏览器第一版是否严格限定研究/阅读，不允许登录后操作？
7. 是否要求所有第三方 DSH 插件先镜像、审计、固定 commit 后再装？

## 12. 参考与核验入口

- DSH 架构：<https://deepseek-harness.github.io/deepseek-harness/en/reference/>
- DSH 官方仓库：<https://github.com/deepseek-ai/deepseek-harness>
- Hermes Agent 的闭环学习参考：<https://hermes-agent.nousresearch.com/docs/>
- modsearch 的 DSH 集成说明：<https://github.com/liustack/modsearch/blob/main/docs/harness-setup.md>
- dsh-ego-browser 社区条目：<https://dsh-plugin.com/plugins/dshworks/dsh-ego-browser>

> 此文档是重建决策与产品路线，不是 DSH 安装授权。任何安装、外部凭据、浏览器控制、旧数据迁移均须在阶段 0 的审计与用户确认后进行。
