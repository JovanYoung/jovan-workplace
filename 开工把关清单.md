# 开工把关清单（PM 前置建议 · 每次开工必走）

> 生效日期：2026-09-06 · 适用范围：任何「对接文档 → 执行会话（Codex/WorkBuddy/Kimi 等）」的开工
> 原则：**执行前先把关，不执行了才发现方向错。**

---

## 一、流程

```
用户说"开工 X 版本"
  → PM 读需求/对接文档，输出【开工建议卡】（四维 + 风险）
  → 用户过目：批准 / 修改 / 补充
  → 执行会话开工（按对接文档 + 建议卡执行）
```

## 二、开工建议卡模板（PM 每次必须输出四维）

```
【开工建议卡 · <版本/任务名>】

1️⃣ 模型选型
   建议：<模型 + 思考档位>（理由：任务复杂度/成本/历史经验）
   备选：<模型>（当主选不可用/想省钱时）

2️⃣ Prompt 设置
   开场模板：读取 <对接文档路径>，先复述 <第N节「开工前复述」>，
            确认后再开工；完成后交交接文档 + 危险操作报告。
   强调要点：<本期最容易跑偏的 1-3 件事>

3️⃣ 架构设计核查
   ⚠️ 红线（别动）：<数据模型/单 resolver/零依赖/前缀缓存/禁 prompt-confirm …>
   🔑 关键假设：<本期依赖的架构事实，错了会翻车>
   📐 数据/迁移：<是否动表/字段；旧数据兼容规则>

4️⃣ 方案正确性（待拍板项）
   | 项 | 默认 | 备选 | 风险 |
   |---|---|---|---|
   | ... | ... | ... | ... |

⚠️ 本期风险：<1-3 条最可能翻车的点 + 缓解>
```

## 三、各维度的检查要点（PM 自查）

### 1. 模型选型
- 任务复杂度：架构重构/数据模型 → 强推理高思考；UI 微调 → 中档；纯文档 → 快档
- 参考历史：同型任务上次用啥成的（M 系列 V4 Pro；1.1 由 Kimi K3 完成 OK）
- 成本：长任务/多轮 → 默认 flash 档，深度环节切 pro

### 2. Prompt 设置
- 必含：读哪个文档、复述哪一节、交什么（交接+危险报告）
- 必强调：范围边界（可写路径唯一）、防蔓延（"明确不做"节）、数据保护
- 检查：执行模型是否见过项目（新模型必须加"先读 Codex-项目交接.md"）

### 3. 架构设计核查
- JW 永久红线：零新 npm 运行时依赖；renderer 禁 window.prompt/confirm；
  数据文件非指令不动；删除先 git status；只写 `D:\Jovan's Workplace\`
- 常见陷阱：ask/plan 单 resolver（并发需隔离）；FTS5 中文走 LIKE；
  前缀缓存（fixedSystem 必须纯常量）；写工具草稿确认制；
  conversations.db 备份已含（WAL checkpoint）；Electron 环境变量注入
- 数据模型变更必须写清：可选字段/旧数据零迁移/写入通道（data.add）

### 4. 方案正确性
- 待拍板项列出 + 默认值（用户没空就按默认跑，回来可改）
- 两个以上可选方案必给对比表 + 推荐 + 理由
- 技术选型先查证（dsh/开源生态动态要核实时效）

## 四、执行会话侧要求

- 开工前：复述「开工前复述」小节（对接文档里有）→ 得到确认
- 执行中：发现对接文档与现状冲突 → 停下问，不擅自改方案
- 完成后：交接文档（验收对照/坑/代码清单）+ 危险操作报告

## 五、配套文档索引

| 文档 | 作用 |
|---|---|
| `Codex-项目交接.md` | 项目全记忆（新执行者必读）|
| `AI功能蓝图.md` | 需求总账 |
| `开工把关清单.md` | 本文（流程规范）|
| 各版本 `*-对接.md` / `*-交接.md` | 历史执行记录 |
