# 1.1 大改版 — 交接文档（执行完成回填）

> 对应需求文档：`1.1-大改版-对接文档.md`
> 执行方：WorkBuddy（外包执行模型）
> 完成日期：2026-09-02
> 提交：`feat(1.1):` 前缀，main 分支

---

## 一、验收清单对照（9/9）

| # | 验收项 | 状态 | 验证方式 |
|---|--------|------|----------|
| 1 | 思考转圈 + 秒数显示 | ✅ | DOM 断言 + 代码审查（见「待 GUI 确认」） |
| 2 | 备忘 4 Tabs + 设置旧入口移除 + 老忘记的事儿下线数据保留 | ✅ | UI 冒烟 13 项 |
| 3 | 导航 4 Tab；主页 = 缩略仪表(可折叠) + Agent；原今日功能可达 | ✅ | UI 冒烟 + 代码审查 |
| 4 | 主 Agent 对话按天落库（当天续聊、跨天新会话） | ✅ | 落库冒烟 7/7 |
| 5 | 聊天记录查看器「日期 → 主/学科」树，点击打开 | ✅ | 落库冒烟 + UI 冒烟 |
| 6 | 日程「当日 Agent 记录」跳转按钮 | ✅ | 代码审查（日视图导航行） |
| 7 | 既有功能回归：学科 AI/解析/detect/记忆自动存/打字机/操作条 | ✅ | M4 回归 12/12 + 静态核查 |
| 8 | 快捷键与设置文案同步 | ✅ | UI 冒烟（settings-nav-clean、nav-4-tabs） |
| 9 | 交接文档交付 | ✅ | 本文档 |

---

## 二、结构改动图

### 导航（改动前 → 改动后）

```
改动前：今日 | Agent | 日程 | 备忘 | 学习        （快捷键 1-5）
改动后：主页 | 日程 | 备忘 | 学习              （快捷键 1-4）
```

### 主页面板（#todayPanel，原「今日」+ 原「Agent」合并）

```
┌ 今日概览 ──────────────────── [折叠按钮 #dashToggle] ┐
│ #homeDash（可折叠，状态存 localStorage wb_home_dash）  │
│  ┌ 四张缩略卡 .today-card（hover 放大 / 点击展开详情）┐ │
│  │ 日程 | 任务 | 备忘(亲友) | 学习                     │ │
│  │ —— 展开为手风琴式单开，原今日四象限内容不丢        │ │
│  └──────────────────────────────────────────────┘ │
│  全局概览卡                                          │
├──────────────────────────────────────────────────┤
│ Agent 对话区（原 #panel-agent 内容整体迁入）          │
│  toolbar: 模型/思考深度/清空/🕘历史(#chatHistoryBtn)  │
│  #chatLog → #thinkBar(思考条) → 输入框               │
└──────────────────────────────────────────────────┘
```

### 备忘面板（#memoPanel）

```
改动前：亲友画像 + 「老忘记的事儿」列表（+ 设置里散落 技能/词典/记忆）
改动后：[亲友画像 | 技能 | 学习词典 | 记忆] 4 Tabs（#memoSeg chips）
        ├─ #memoPaneFamily  ← 原 renderFamily() 不动
        ├─ #memoPaneSkills  ← 从设置搬入 #memSkillList / #memAddSkill
        ├─ #memoPaneDict    ← 从设置搬入 #setDictCount / #setDictClear
        └─ #memoPaneMemory  ← 从设置搬入 #memFactList / #memAddFact / #memFactEdit
模块名常量：MODULE_MEMO_NAME = '备忘'（占位；定名后只改这一处，applyModuleName() 全量刷新 .memo-name 元素）
「老忘记的事儿」UI 已下线；数据文件未删（数据保留原则）。
设置侧栏移除：智能解析、记忆 两项（#panel-parse / #panel-memory 面板删除）。
```

### 聊天记录体系（D 块）

```
conversations.db（复用 conv.js，不改表结构、不开新存储）
├─ subject='主Agent'   title=YYYY/M/d（当天日期）
│    · 启动时 restoreTodayConv() 找回当天会话并渲染历史
│    · 当天续用；跨天/手动清空后 ensureMainConv() 自动新建
│    · 每条消息经新 IPC conv:append 落库（user/assistant 按序）
└─ subject=学科名      （原有逻辑不动）

查看器 #historyMask（主页 🕘历史 / 日程 📅当日记录 两个入口）
  日期（按 updated_at 本地日期分组，倒序）
   ├─ 主Agent · 标题
   └─ 学科 · 标题
  点击：主Agent → loadMainConv() 载入主页；学科 → openSubjectAiConv()
  日程跳转：jumpDayAgentLog() 定位当日分组并高亮；无记录 toast 提示
```

---

## 三、新 UI 说明

| 功能 | 位置 | 说明 |
|------|------|------|
| 思考条 | 主页 `#thinkBar` / 学科 `#subjectThinkBar` | `⟳ 思考中 Ns`：发送即出现、每秒跳动；主 Agent 在工具循环期间累计不清零，首个输出/done 事件消失；学科 SSE 首个 delta 消失；失败/超时同样消失并报错 |
| 缩略仪表盘 | 主页 `#homeDash` | 右上角按钮折叠/展开，状态持久化；卡片 hover 放大、点击手风琴展开详情（排除卡片内按钮点击） |
| 历史查看器 | `#historyMask` modal | 树状分组，当天分组高亮 + scrollIntoView |
| 备忘 4 Tabs | `#memoSeg` | 懒渲染：切到哪个 pane 才渲染哪个 |
| 快捷导航徽章 | memo badge | 改数「亲友」数量（原老忘记的事儿已下线） |

---

## 四、代码清单

| 文件 | 改动 | 摘要 |
|------|------|------|
| `app/renderer/index.html` | +499/-130（净） | 全部 UI 重组 + 思考动画 + 落库 + 查看器；新增 CSS：`.think-bar`/`@keyframes thinkspin`/`.today-card.expanded`/`.tc-list` 限高/`.dash-toggle`/`.his-*` 树样式 |
| `app/main.js` | +8 | 新增 IPC `conv:append`（校验会话存在 → `conv.appendMessage`） |
| `app/preload.js` | +2 | 暴露 `convAppend(id, role, content)` |

渲染器关键新增函数：
- `thinkStart(barId)` / `thinkStop()` — 单全局 `thinkTimer`，1s 间隔
- `MODULE_MEMO_NAME` / `applyModuleName()`
- `ensureMainConv()` / `loadMainConv(id)` / `restoreTodayConv()` / `convDateStr(ts)`
- `openHistory(focusDate)` / `jumpDayAgentLog()`
- `refreshDictCount()`；`renderMemo()` 简化为 `renderFamily()`

顺手修复既有 bug：`renderShortcutPanel` 重复定义（后定义覆盖前者导致快捷键设置列表不渲染），已删除重复实现。

未做（按需求明确排除）：语音输入 / 周期性任务 / 真流式 Agent / 仪表盘视觉大改 / 表结构变更。零新 npm 依赖。

---

## 五、遇到的坑（4 个执行坑 + 1 个环境坑）

1. **Edit 误删函数头**：替换 `renderMemo` 时 old_string 带入了 `renderFamily` 开头两行未补回，`check-html.js` 报 `Unexpected token 'function'`。教训：大范围替换后立即跑 `check-html.js` 而不是攒到最后。
2. **HTML 闭合标签误带**：memo 面板替换末尾多写 `</main></div>` 导致结构重复。用 grep 面板行号序列（todayPanel → memo → learn → `</main>`）验证结构修复。
3. **`ensureMainConv` 清空语义漏洞**：`chatClear` 后 `mainConvFresh=true`，但原实现先重置 flag 再查当天会话，会错误复用旧会话。修复：先快照 `wasFresh` 再重置，`wasFresh` 时跳过复用直接新建。
4. **Electron 冒烟 stdout 静默（环境坑，新）**：Git Bash 下脚本一旦创建 `BrowserWindow`（哪怕 `show:false` 不加载页面），stdout 完全静默、EXIT=0——进程其实活着，是 stdout 管道失效。无窗口脚本输出正常。**解法：日志改走 `fs.writeSync(fd, ...)` 写文件**，全部断言正常落盘。后续所有带窗口的冒烟脚本沿用此模式。
5. **about 页 Edit 缩进不匹配**：old_string 首行缩进与实际不符导致替换失败，从内容行起替换绕开。

---

## 六、测试记录

| 测试 | 结果 |
|------|------|
| `node check-html.js`（内联脚本语法） | OK block#1 (180735 chars) |
| `node --check` main/preload/conv | 全部通过 |
| 落库冒烟（主Agent 建会话/消息顺序/当天查找/学科隔离/清空后最新优先/updated_at） | 7/7 PASS |
| M4 记忆回归（mem-smoke.js：facts/skills/注入） | 12/12 PASS |
| UI 冒烟（隐藏窗口加载 index.html，13 项 DOM 断言：thinkBar×2/homeDash/historyMask/4+4 tabs/memo panes/搬入控件/设置导航净化/旧元素移除/卡片数/chat 在主页/零脚本错误） | 13/13 PASS |

冒烟脚本均为一次性用品，已删除，未入库。

---

## 七、待用户本地 GUI 确认项（需真实 API Key，headless 无法覆盖）

1. 发一条需工具调用的复杂问题 → 主页看 `⟳ 思考中 Ns` 跳动、出字后消失（验收项 1 的实机观感）。
2. 学科对话发一条 → 学科思考条同样行为，SSE 流式不受影响。
3. 重启应用 → 主页应自动恢复当天主 Agent 历史；手动改系统日期到明天再发一条 → 查看器里出现新日期分组。
4. 主页卡片 hover/点击展开、折叠按钮记忆；日程日视图「📅 当日记录」跳转与高亮。
5. 打字机效果与消息操作条（复制/朗读/重生成）实机回归。
6. 备忘模块定名后：只改 `renderer/index.html` 顶部 `MODULE_MEMO_NAME` 常量一处即全量生效。

---

## 八、给下一棒的备注

- 主 Agent 落库是纯渲染器侧实现（convList/convCreate/convAppend 组合），若后续要加「会话重命名/删除」，直接复用 conv.js 现有 API + 在 `#historyMask` 树节点加按钮即可。
- `mainConvId/mainConvFresh` 是主页会话的单一事实源；新增清空入口时务必同步 `mainConvId=null; mainConvFresh=true; thinkStop();`（参照 `chatClear`）。
- 思考条计时器是单全局 `thinkTimer`，两处 bar 共用；若未来允许主页/学科并发对话，需改为 per-bar 计时器。
