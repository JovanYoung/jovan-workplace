# Jovan's Workplace · 1.0.0 M2 开工对接文档（工具循环 + 一句话解析 + 全模块主动检测）

> 唯一交接文档。**开工前先完成第九节「五问复述」并得到确认。**
> 需求总账见同目录 `AI功能蓝图.md`（v2）；M1 已完成（AI 网关，commit e59f6ec）。

---

## 一、当前状态（M1 已完成，M2 在此基础上做）

```
app\
├── main.js       主进程：窗口/托盘/数据目录/ima key/IPC
│                 ✅ 已有 ai:list-providers / ai:save-key / ai:test / ai:chat（流式）
├── ai.js         ✅ AI 网关：8 厂商模板 + deepseek-v4-flash/pro + 价格表
│                 ✅ chat() 流式（SSE，含 reasoning_content 跳过 + include_usage 回退）
│                 ✅ Key safeStorage 加密
├── preload.js    ✅ window.workplace.aiListProviders/aiSaveKey/aiTest/aiChat
├── data.js       ✅ 数据层，导出：add/update/del/mutate/rows/findRow/logOp/...
│                 ⚠️ 工具循环可直接在主进程调用（不需走 renderer IPC）
└── renderer\index.html
                  ✅ 设置页「AI 模型」面板 + Agent 基础对话框（token/RMB 显示）
                  ⚠️ 一句话解析仍走 8787 桥（BRIDGE='http://127.0.0.1:8787'，aiDeepBtn）→ M2 迁移
```

**关键架构事实**：主进程 `require('./data.js')` 可直接 `data.add/update/del/mutate`——**AI 工具的执行发生在主进程，直接调数据层**，无需经 renderer 中转。

---

## 二、M2 范围（五步）

### Step 1：工具循环引擎（扩展 ai.js）
- [ ] `runAgentLoop({ provider, model, messages, tools, onEvent })`：
  - [ ] 请求带 `tools`（OpenAI 兼容格式：name/description/parameters JSON schema）
  - [ ] 模型返回 `tool_calls` → 逐个执行 → 结果回填 `role:'tool'` → 再请求，直到无 tool_calls
  - [ ] **循环上限** `MAX_TOOL_ROUNDS = 5`（config.json 可调）
  - [ ] **结果截断**：工具返回值超 2000 字符截断（防上下文爆炸）
  - [ ] **并行工具**：一次多个 tool_calls 先并行执行再合并回填
  - [ ] **失败容错**：工具抛错 → 错误信息回填，重试最多 1 次
  - [ ] 每轮状态通过 `onEvent` 推送（`webContents.send('ai:agent-event', ...)`），供 UI 显示步骤条

### Step 2：工作台工具集（主进程注册，直接调 data.js）
- [ ] **读工具**（直接执行）：
  | 工具 | 说明 |
  |---|---|
  | `query_today` | 今日待办/逾期/生日概览 |
  | `query_schedules` | 查日程/任务（按日期/关键字）|
  | `query_memos` | 查备忘 |
  | `query_family` | 查亲友画像 |
  | `query_study` | 查学习（学期/科目/备考）|
- [ ] **写工具**（**返回草稿，不直接执行**——安全底线）：
  | 工具 | 说明 |
  |---|---|
  | `create_schedule` | 草稿 {标题,日期,时间,地点,优先级,详情} |
  | `create_task` | 草稿 {标题,优先级,日期} |
  | `create_memo` | 草稿 {标题} |
  | `create_family` | 草稿 {姓名,关系,忌口,雷点,喜好,生日} |
  | `create_study` | 草稿 {学期/科目/备考} |
  - 写工具执行后返回 `{draft: true, ...}` → 由 UI 弹确认框 → 用户确认后真正 `data.add`
- [ ] 工具描述用中文写清楚"何时用、参数含义"，帮助模型选对工具

### Step 3：一句话解析迁移（弃 8787 桥）
- [ ] 新 IPC `ai:parse {text}` → 主进程调内置 LLM：
  - [ ] **JSON Output** 强制 schema：`{类型: 日程|任务|备忘|亲友|学习|备考, 标题, 日期, 时间, 地点, 优先级: 紧急且重要|紧急不重要|重要不紧急|都不重要, 标签, 详情}`
  - [ ] 用 `deepseek-v4-flash`（便宜快），JSON schema 校验失败重试 1 次
  - [ ] 返回 `{ok, parsed, confidence}`；低置信度时提示用户可走「AI 深度解析」（用 pro）
- [ ] **学习词典迁移**：从 localStorage（`wb_parse_dict`）迁到 `data\dict.json`（主进程读写，学习习惯跨设备）
- [ ] renderer：qInput 一句话添加 + aiDeepBtn 改走 `ai:parse`；删除 8787 BRIDGE 引用（页面中 `BRIDGE` 变量与 fetch 调用）
- [ ] 机械解析逻辑（现有本地规则）**保留作兜底**：AI 不可用时退回

### Step 4：F1 全模块主动检测（对话后意图检测）
- [ ] 每次 Agent 对话结束后（或新消息后），追加一次轻量检测（flash 模型）：
  - [ ] 检测 prompt：分析最近对话，是否出现 8 类可执行意图（日程/任务/备忘/亲友/学习/冲突/逾期/生日）
  - [ ] 命中 → 生成结构化草稿 → IPC 推送 renderer → **弹确认框**（类型 + 内容预览 + [确认][忽略]）→ 用户确认 → 执行 `data.add/update`
- [ ] 8 类意图与工具映射（复用 Step 2 的写工具）：
  | 意图 | 例子 | 写入 |
  |---|---|---|
  | 日程 | "下周约导师" | create_schedule |
  | 任务 | "周五交计网作业" | create_task |
  | 备忘 | "别忘了我妈不吃香菜" | create_memo |
  | 亲友 | "我室友养了猫叫团子" | create_family |
  | 学习 | "这周开始刷雅思听力" | create_study |
  | 冲突 | 新日程撞已有日程 | 提示不写入 |
  | 逾期 | 昨天没做完的事 | 提议顺延 |
  | 生日 | 亲友生日临近 | 提醒不写入 |
- [ ] 确认框 UI 风格与现有弹窗一致（毛玻璃 + 弹性）

### Step 5：思考深度滑块
- [ ] **实测** V4 思考模式参数（`reasoning_effort` 或等效字段；关闭思考用 FIM/非思考模式的准确参数）——实测后写进交接文档
- [ ] 滑块 UI（Agent 对话框，模型选择旁）：关闭 / 浅 / 标准 / 深 → 注入 `chat()` 请求参数
- [ ] 与 token/RMB 费用显示联动（调深时费用变化可见）

---

## 三、明确不做（防范围蔓延）

- ❌ Mem0 记忆（M3）
- ❌ 学科 AI / 历史搜索（M3）
- ❌ Galgame 分叉对话（M4+）
- ❌ 课件翻译新 UI / 图片解析 / 截图讲题（M4）
- ❌ 视觉模型接入（M4）
- ❌ 不重新打包发行（M2 只开发验证，打包统一在 1.0.0 收尾做）

---

## 四、技术约束

- 纯 JS，**不新增 npm 依赖**（fetch 主进程原生可用；JSON Output 用模型参数实现）
- OpenAI 兼容：tools 格式按官方 chat completions 的 function calling 规范
- 数据写入统一走 `data.js` 导出函数（add/update/del/mutate），**不要绕过 data.js 直接写 workspace.json**
- 代码/注释英文；UI 文案中文
- Key 加密沿用 safeStorage（M1 已有模式）
- **思考深度参数、JSON schema 等所有实测结论必须写进交接文档**

## 五、验收标准（全部满足 + 交接文档交付 = M2 完成）

1. 一句话添加：「周五交计网作业，紧急」→ 解析出 {类型:任务, 标题:计网作业, 日期:本周五, 优先级:紧急} → 确认写入成功 ✅
2. 对话：「下周想约导师聊项目」→ 弹日程确认框（含内容预览）→ 确认后写入日程 ✅
3. 对话：「别忘了我妈不吃香菜」→ 弹备忘确认框 → 确认后写入备忘 ✅
4. 构造工具循环（问一个需要多次查数据的复合问题）→ 步骤条可见、正常收敛、不死循环 ✅
5. 滑块可调 → 请求参数变化 → 费用显示联动 ✅
6. 手动添加日程/任务等旧功能不受影响（回归）✅
7. **交接文档必须交付**：记录 reasoning 参数实测结果、工具 schema 清单、学习词典迁移情况、遇到的坑、代码改动清单 ✅（未交付 = 未完工）

## 六、执行顺序（每步能跑再继续）

1. 读 `AI功能蓝图.md`（v2）+ 现有 `ai.js` / `main.js` / `data.js` / renderer 相关段（qInput、aiDeepBtn、BRIDGE）
2. Step 1 工具循环引擎（ai.js 扩展 + 事件推送）
3. Step 2 工具集（读工具 + 写工具草稿模式）
4. Step 3 一句话解析迁移（ai:parse + 学习词典迁移 + renderer 改接 + 删 BRIDGE）
5. Step 4 主动检测（检测 prompt + 确认框 UI + IPC 推送）
6. Step 5 深度滑块（实测参数 → 滑块 → 联动）
7. 本地 `env -u ELECTRON_RUN_AS_NODE npx electron .` 全量回归 + 验收 7 项
8. **写交接文档**（给主 agent 审阅）→ 提交推送

## 七、环境坑（WorkBuddy 沙盒注入，必看）

- 打包/安装命令前加 `NODE_OPTIONS=`（genie-safe-delete shim 劫持 fs.rm）
- 跑 Electron 前 `env -u ELECTRON_RUN_AS_NODE`（否则 require('electron') 返回路径字符串）
- 沙盒 GPU 报错仅测试环境，真实桌面不存在，忽略
- git push 网络波动：重试；必要时 `git -c http.proxy= -c https.proxy= push`

## 八、其他

- 本地未部署 Ollama——**保留模板**，不删（用户可能以后装）
- 完成后 commit message 遵循 `feat(ai): M2 ...` 格式

## 九、开工前复述（五问，全部答对再写代码）

1. M2 要交付的核心是什么（一句话）？
2. 写操作工具怎么保证安全（为什么不能直接写库）？
3. 工具执行的数据通路是什么（主进程还是 renderer）？
4. 一句话解析用什么模型 + 什么输出机制？
5. M2 完成后除了代码还要交付什么（必交）？

---

*交接日期：2026-09-01 · 建议执行模型：DeepSeek V4 Pro（新会话）*
