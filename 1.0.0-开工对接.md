# Jovan's Workplace · 1.0.0 开工对接文档（M1：AI 网关最小闭环）

> 唯一交接文档：所有决策已定稿，执行者无需询问历史背景。
> **开工前必须先完成第八节「五问复述」并得到用户确认，再动手。**
> 完整需求总账见同目录 `AI功能蓝图.md`（v2）——本文件是 M1 的执行范围。

---

## 一、项目背景

- **产品**：Jovan's Workplace 个人效率工作台（已发行 v0.0.0 桌面端）
- **技术栈**：Electron 44 + 纯 JS（无框架）+ electron-builder 26 打包绿色 zip
- **仓库**：`D:\Jovan's Workplace\`（git 已推 GitHub：github.com/JovanYoung/jovan-workplace）

### 当前代码结构（M1 要改动的范围）

```
app\
├── main.js        (195 行)  主进程：窗口/单实例锁/托盘/数据目录选择/ima key 管理/IPC
├── preload.js     (19 行)   contextBridge 暴露 window.workplace（12 个 API）
├── data.js        (379 行)  数据层：原子写入/快照/备份30份/农历/ima 备份
├── renderer\index.html (3632 行) 页面（五大模块 + 设置）
└── assets\        icon.png / icon.ico
```

### 现有 preload API（window.workplace.*）
`loadData` `mutate` `birthdays` `conv` `backupNow` `imaHasKey` `imaSetKey` `imaBackup` `exportData` `importData` `notify` `getDataPath`

### 现状缺陷（M1 要解决的核心）
- **AI 功能依赖外部 dsh 桥**（127.0.0.1:8787/3080），别人装完 AI 全废
- 目标：**弃 dsh，主进程内置 AI 网关**，用户设置页只填 API Key 即可用

---

## 二、M1 范围（第一小步：AI 网关最小闭环）

> 只做这三件事，做完验收通过再进 M2。**不要顺手做 M2+ 的功能。**

### Step 1：主进程 AI 网关（main.js 新增，或新建 ai.js 由 main.js 引用）
- [ ] **厂商模板表**（内置常量，全部 OpenAI 兼容 `/chat/completions`）：

| 厂商 | baseURL | 默认模型 | 备注 |
|---|---|---|---|
| deepseek | `https://api.deepseek.com/v1` | `deepseek-chat` | 文本主力 |
| zhipu | `https://open.bigmodel.cn/api/paas/v4` | `glm-4.5-flash` | 视觉 `glm-4.5v-flash`（免费）|
| moonshot | `https://api.moonshot.cn/v1` | `moonshot-v1-8k` | 备选 |
| openai | `https://api.openai.com/v1` | `gpt-4o-mini` | 备选 |
| qwen | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` | 备选 |
| volcengine | `https://ark.cn-beijing.volces.com/api/v3` | `doubao-1-5-pro` | 备选 |
| siliconflow | `https://api.siliconflow.cn/v1` | `Qwen/Qwen2.5-7B-Instruct` | 免费模型 |
| ollama | `http://localhost:11434/v1` | `llama3` | 本地 |

- [ ] **模型价格表**（每百万 token 的 RMB 单价，input/output 分开）——按各厂商官网公开定价填；注释标明"价格随官方调整，集中在模板表顶部便于更新"
- [ ] `chat()` 核心函数：接收 {provider, model, messages, stream} → 请求对应 baseURL → **支持流式**（SSE 解析）→ 返回/推送增量
- [ ] **Key 配置管理**：存 `config.json`（主进程配置目录），Key 用 `safeStorage.encryptString` 加密落盘（沿用现有 ima key 的套路，参考 main.js 现有代码）
- [ ] IPC（新增加在 main.js）：
  - `ai:list-providers` → 返回模板表（不返回 Key）
  - `ai:save-key` {provider, apiKey} → 加密存储
  - `ai:test` {provider} → 用已存 Key 发一条最短请求验证连通，返回 ok/错误信息
  - `ai:chat` {provider, model, messages} → 流式推送（`webContents.send('ai:chunk', ...)`）+ 结束时返回 {usage, cost}
- [ ] preload.js 新增：`aiListProviders` `aiSaveKey` `aiTest` `aiChat`（+ 流式监听封装）

### Step 2：设置页多模型 Key 管理 UI（renderer/index.html 设置模块）
- [ ] 设置 → 新增「AI 模型」面板（左侧菜单加一项）
- [ ] 厂商列表（模板表 8 家），每家一行：厂商名 + Key 输入框（密码态）+ [测试] 按钮 + 已配置状态点
- [ ] **只填 Key，不暴露 baseURL/模型名配置**（预置模板里写死）
- [ ] 视觉模型默认标注：智谱 GLM-4.5V-Flash（免费）

### Step 3：最小对话闭环（验收入口）
- [ ] Agent 模块（或临时入口）加一个基础对话框：模型下拉（列出所有已配置 Key 的厂商模型）+ 输入框 + 流式输出区
- [ ] system prompt：简单版「你是 Jovan 的个人工作台助手，回答简洁专业，中文优先」
- [ ] **每次对话结束显示：本次 token 用量（input/output）+ 折算 RMB 费用**（按价格表实时算）
- [ ] 不做：工具循环/主动检测/学科 AI/galgame/记忆（那是 M2+）

---

## 三、明确不做（防止范围蔓延）

- ❌ 工具循环（M2）
- ❌ Agent 主动检测/确认框（M2）
- ❌ 一句话解析迁移（M2）
- ❌ Mem0 记忆（M3）
- ❌ 学科 AI / 历史搜索（M3）
- ❌ 课件翻译 UI / 图片解析 / 截图讲题（M4）
- ❌ 修改现有数据层（data.js 不要动，除非 M1 必须）

---

## 四、技术约束

- 纯 JS，**不引任何外部依赖**（不装 npm 包；fetch 在 Electron 主进程原生可用）
- OpenAI 兼容接口：`POST {baseURL}/chat/completions`，Authorization: Bearer {key}
- 流式：`stream: true`，解析 `data: {...}` 行（SSE），`[DONE]` 结束
- 网络请求走主进程（renderer 无跨域限制问题）
- 代码/注释英文；UI 文案中文
- Key 加密沿用现有 ima key 的 safeStorage 模式（读 main.js 里的 ima 代码照抄模式）

## 五、验收标准（全部满足才算 M1 完成）

1. 设置 → AI 模型：填入 DeepSeek Key → [测试] 显示连通 ✅
2. 对话框选 DeepSeek → 发"你好" → 流式回复 ✅
3. 对话结束显示 token 用量 + RMB 费用（金额 > 0 且合理）✅
4. 重启应用 Key 仍在（safeStorage 解密成功）✅
5. 填错 Key → [测试] 给出清晰错误提示，不崩 ✅
6. 页面其他功能（日程/备忘/学习/设置其余项）**不受影响**（回归）✅

## 六、执行顺序（每步能跑再继续）

1. 读 `AI功能蓝图.md`（v2）总览 + 读现有 `main.js`（学 ima key 模式）/ `preload.js` / `data.js`
2. main.js（或新 ai.js）：厂商模板 + 价格表 + chat() + Key 管理 + IPC
3. preload.js 暴露新 API
4. renderer 设置页「AI 模型」面板
5. renderer Agent 基础对话框 + 流式 + token/RMB 显示
6. 本地 `electron .` 跑通验收 6 项
7. （不打包！M1 只开发验证，打包留给整体 1.0.0 完成后统一做）

## 七、环境坑（必看，WorkBuddy 沙盒注入）

- **打包/安装命令前加 `NODE_OPTIONS=`**（环境注入了 genie-safe-delete shim 劫持 fs.rm → 报 safe-delete 失败）
- **跑 Electron 前 `env -u ELECTRON_RUN_AS_NODE`**（环境注入该变量会让 require('electron') 返回路径字符串 → app undefined）
- 沙盒无 GPU 会报 `GPU process isn't usable`——仅测试环境，真实桌面（RTX 4060）不存在，忽略
- git push 若失败：网络波动，重试即可；全局代理 127.0.0.1:7897 常不开，必要时 `git -c http.proxy= -c https.proxy= push`

## 八、开工前复述（五问，全部答对再写代码）

1. M1 要交付的最小闭环是什么（一句话）？
2. 厂商 API 配置是硬编码模板还是让用户配 baseURL？
3. API Key 存哪里、怎么加密？
4. 流式响应怎么实现（技术细节）？
5. M1 明确不做哪三类功能？

---

*交接日期：2026-09-01 · 建议执行模型：DeepSeek V4 Pro（新会话）*
