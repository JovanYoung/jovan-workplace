# Jovan's Workplace · 1.0.0 M4 开工对接文档（L3 事实记忆 + G2 自动技能提炼）

> 唯一交接文档。**开工前先完成第九节「五问复述」并得到确认。**
> 需求总账见 `AI功能蓝图.md`（v3.1）；M1/M2/M3 已完成（commit a91e249）。

---

## 一、当前状态（M3 已完成，M4 在此基础做）

```
app\
├── main.js      窗口/托盘/数据目录/IPC（data:*, ai:*, conv:*）
├── ai.js        AI 网关：8 厂商/流式/runAgentLoop(工具循环)/jsonOutput/THINKING_LEVELS
├── tools.js     10 工具（5 读 + 5 写草稿）+ normalizeDate/normPriority/normType
├── parse.js     一句话解析 + detect 8 类意图 + 学习词典 + todayCN + pickDefaultModel(pro)
├── conv.js      ✅ SQLite 会话管理器（conversations.db，WAL）+ FTS5/LIKE 搜索 + 窗口压缩 L1 + studyMaterials 档案注入
├── data.js      数据层（workspace.json）
├── preload.js   window.workplace.*（含 conv*/ai*/dict*）
└── renderer\index.html  五大模块 + Agent 对话 + 学科 AI modal + 思考滑块/步骤条/draft 确认卡
```

**记忆体系现状**：L1 窗口压缩 ✅（conv.js）/ L2 FTS5 历史搜索 ✅ / L4 雏形 ✅（studyMaterials 档案注入）
**M4 补上**：**L3 事实记忆（带可追溯 source）+ G2 自动技能提炼 + 注入机制 + 管理 UI**

---

## 二、M4 范围（四步）

### Step 1：L3 事实记忆库（mem.js 新增）

> **PM 决策（重要）：L3 不用 Mem0，自建轻量事实库**。理由：①偏好事实是少量结构化数据（几十条级），不需要向量检索 ②需要 source 可追溯 + 可编辑删除，Mem0 不灵活 ③零依赖铁律（Mem0 JS 需向量库，不成熟）④Hermes/DeepTutor 证明自建分层完全够用。此决策如有异议可向用户提出，默认照此执行。

- [ ] 存储：**复用 `conversations.db`**（WAL 已开，多连接安全）新增 `facts` 表（mem.js 独立 `DatabaseSync` 打开同路径，或直接扩展 conv.js——选实现简单且不破坏 conv 单例的方案）：
  ```sql
  CREATE TABLE IF NOT EXISTS facts (
    id TEXT PRIMARY KEY,
    fact TEXT NOT NULL,              -- 事实内容（"用户雅思备考中"）
    category TEXT DEFAULT 'pref',    -- pref=偏好 / correct=纠正 / habit=习惯 / background=背景
    source_conv TEXT,                -- 证据：来源会话 id（可空=手动添加）
    source_ts INTEGER,               -- 证据：时间戳
    status TEXT DEFAULT 'active',    -- active / dismissed（用户忽略/删除=软删）
    created_at INTEGER
  );
  ```
- [ ] **提炼触发**：Agent 对话结束后（或 detect 同时机），用 flash 做轻量「事实提炼」检测：从最近对话提取「值得跨会话记住的信息」（你宣布的偏好/习惯/纠正过的错误），返回候选列表
- [ ] **确认机制（不自动记）**：候选事实 → 弹确认卡「🧠 要记住吗：'你雅思备考中' [记住][忽略]」→ 记住才写 facts 表（带 source_conv/source_ts）——参考 draft 确认卡的成熟模式
- [ ] **注入机制**：会话开始时加载 active 事实（按 category 分组、可截断最相关 N 条）进 system prompt：`【关于 Jovan 的记忆】…`——通用 Agent 会话 + 学科会话都注入（学科会话先注入学科相关）
- [ ] 手动管理：IPC `mem:add` / `mem:list` / `mem:delete` / `mem:confirm`（确认卡回写）

### Step 2：G2 自动技能提炼（skills.js 新增）

- [ ] **技能格式**（借鉴 Hermes skill / DeepTutor）：`{ id, name, 场景, 步骤[], 坑[], 验证, category, hits, created_at }` 存 `data\skills\{id}.json`（文档文件，便于导出/编辑/版本管理）
- [ ] **触发**：runAgentLoop 完成 ≥3 轮工具调用 **或** 用户对话中修正 ≥1 次 → 完成后自动生成「技能提炼建议」→ 弹确认卡「🛠 要把这次做法存成技能吗？[存][不存]」
- [ ] **提炼**：flash 读最近对话/工具轨迹 → 输出 场景/步骤/坑/验证 结构化（jsonOutput 复用）
- [ ] **命中注入**：任务/对话开始前按「场景关键词」匹配已有技能（简单包含匹配 + 学科分类）→ 命中则注入 system prompt：「以下技能可能适用：{技能名}（场景摘要）」
- [ ] **两级沉淀**：轻量习惯（"妈妈说=亲友画像"）继续走学习词典 dict.json；结构化流程走技能库——M4 只做技能库，词典不动
- [ ] IPC：`skill:list` / `skill:save` / `skill:delete` / `skill:toggle`（禁用）

### Step 3：记忆管理 UI（设置页新增「记忆」面板）
- [ ] **事实列表**：每条显示 内容 + 分类标签 + 来源（"来自 数学会话 · 09-02"）+ [删除]——**点来源跳回对应会话**（复用 conv:search/open，Memory Graph 简化版可追溯）
- [ ] **技能列表**：技能名 + 场景摘要 + hits（调用次数）+ [查看/编辑][删除][禁用开关]
- [ ] 从记忆面板能直接添加事实/技能（手动兜底，不依赖 AI 提炼）

### Step 4：注入接线 + 端到端收口
- [ ] conv.js 的 buildSystemPrompt 追加「记忆注入」：facts（active，按会话相关性）+ 命中技能
- [ ] Agent 对话（ai:agent）同样注入
- [ ] 防抖：同一候选事实已确认/忽略（60s 内）不重复弹（复用 M3 detect 去重模式）

---

## 三、明确不做（防范围蔓延）

- ❌ Mem0 / 向量库（决策见 Step 1）
- ❌ Galgame 分叉 / 课件翻译新 UI / 图片解析 / 截图讲题（M5+）
- ❌ 语义向量搜索 sqlite-vec（后续）
- ❌ 掌握度追踪 / 教学模式开关（蓝图 E 标注"可选"，M5+ 再做）
- ❌ 不重新打包发行（1.0.0 收尾统一做）

## 四、技术约束

- 纯 JS + node:sqlite（已有先例），**零新 npm 依赖**
- facts 表 SQLite；skills 用 JSON 文件（可编辑导出）
- AI 提炼复用 ai.js（chatOnce/jsonOutput + flash 模型）
- 记忆/技能属高隐私 → 只落本地，不上 ima（沿用决策）
- 代码/注释英文；UI 文案中文

## 五、验收标准（全满足 + 交接文档 = M4 完成）

1. 对话说「以后每周三提醒我开组会」→ 弹「🧠 要记住吗」→ 记住 → **新开会话**问「我周三有什么安排」→ 回答带出组会事实 ✅
2. 对话纠正「我不是本科生，我是研究生」→ 提炼出纠正事实（category=correct）→ 之后回答不再叫错 ✅
3. 触发一次 ≥3 轮工具调用的任务 → 完成后弹「🛠 存成技能？」→ 存后 skills.json 有结构化技能（场景/步骤/坑/验证）✅
4. 设置 → 记忆：事实列表可见（含来源），点来源跳回原会话；技能可编辑/删除/禁用 ✅
5. 已忽略的候选不重复弹（60s 内）✅
6. 既有功能回归：学科 AI 问答 / Agent 对话 / 一句话解析 / 手动功能全不受影响 ✅
7. 交接文档交付（facts/skills 结构、提炼实测样例、注入效果对比、坑、改动清单）✅

## 六、执行顺序（每步能跑再继续）

1. 读 `AI功能蓝图.md`（v3.1）G/G2 段 + conv.js（学习 init/dbPath/buildSystemPrompt/draft 确认模式）+ parse.js detect
2. Step 1 facts 表 + mem.js（CRUD/提炼/确认/注入）
3. Step 2 skills.js（格式/触发/提炼/命中注入）
4. Step 3 设置「记忆」面板 UI
5. Step 4 注入接线（conv buildSystemPrompt + Agent）
6. 本地跑通验收 7 项（真实 key 已有效）
7. **写交接文档**（必交）→ 提交推送（`feat(m4):` 前缀）

## 七、环境坑（沙盒注入，照抄 M2/M3）

- Electron 脚本直接跑：`app.setName("Jovan's Workplace")` + `setPath('userData', "C:\\Users\\17169\\AppData\\Roaming\\Jovan's Workplace")` 在 whenReady 前（safeStorage 解密坑）
- 命令行：`env -u ELECTRON_RUN_AS_NODE NODE_OPTIONS= ./node_modules/.bin/electron xxx.js --no-sandbox --disable-gpu --disable-gpu-compositing`
- 测试脚本**禁止硬编码 API Key**（从 `process.env.JW_TEST_KEY` 读；用完删除）——上轮已踩 Key 泄漏风险
- git push 网络波动重试 / `-c http.proxy=` 绕过

## 八、交接文档要求（必交）

1. facts 表 / skills.json 结构定稿
2. 事实提炼 + 技能提炼的实测样例（真实 LLM 输出）
3. 记忆注入前后回答对比（有没有注入的区别样例）
4. 遇到的坑 + 代码改动清单

## 九、开工前复述（五问，全对再动手）

1. M4 交付什么（一句话）？
2. L3 为什么不用 Mem0（说三条理由）？
3. 事实/技能存哪里、什么格式？
4. 怎么保证"AI 记错了能发现"（可追溯怎么实现）？
5. 记忆确认卡和技能确认卡的触发时机分别是什么？

---

*交接日期：2026-09-02 · 建议执行模型：DeepSeek V4 Pro（新会话）*
