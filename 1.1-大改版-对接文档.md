# Jovan's Workplace · 1.1 大改版对接文档（外包执行）

> 唯一交接文档 · 交付对象：Kimi K3（或任意执行模型）· 日期 2026-09-02
> **开工前先完成第九节「九问复述」并得到用户确认。**
> 代码基址：`D:\Jovan's Workplace\app`（Electron 44 + 纯 JS 零依赖，git 在 `D:\Jovan's Workplace` main 分支）

---

## 〇、一句话总览

本次是**模块重组 + 会话体系升级**，不是新功能堆叠：①Agent 思考动画 ②备忘模块瘦身+收纳（去"老忘记的事儿"，并入技能/词典/记忆，改名待定）③今日与 Agent 合并成「主页」④聊天记录改为"日期 → 主/学科"树状体系并在日程可跳转。

---

## 一、现状速览（先读这些代码）

```
app\
├── main.js       IPC（data:* / ai:* / conv:* / mem:* / skill:*）
├── ai.js         AI 网关（runAgentLoop 工具循环/thinking 滑块/chat 流式）
├── conv.js       学科会话管理器（SQLite conversations.db：conversations/messages/facts 表 + FTS5）
├── mem.js        事实提炼（flash）
├── skills.js     技能库（data\skills\{id}.json）
├── parse.js      一句话解析 + detect + 学习词典（data\dict.json）
├── data.js       workspace.json 数据层
├── preload.js    window.workplace.*
└── renderer\index.html  (~460KB) 五大面板（today/schedule/memo/learn/agent）+ 设置（外观/数据/日志/回收站/AI 模型/记忆/智能解析/快捷键/关于）
```

**关键现状事实**：
- 导航 5 Tab：今日 / 日程 / 备忘 / 学习 / Agent（`data-tab` 切换，快捷键 1-5）
- **主 Agent 对话纯内存（chatHistory 不落库）**；学科对话才落 conversations.db（subject=学科名）
- 备忘面板 = memoList（老忘记的事儿）| familyList（亲友画像）两个子页切换
- 技能/学习词典/记忆目前都收在「设置」里（记忆=panel-memory、智能解析=panel-parse、技能在记忆面板）
- Agent 对话有：打字机效果 / Markdown 渲染 / 消息操作条（复制/朗读/重生成）/ 工具步骤条 / 思考滑块 / draft 确认卡 / 记忆自动存
- 无语音输入（Electron 内 Web Speech 识别不可用）——**本期不改**

---

## 二、A. Agent 思考状态动画（小，先做练手）

**目标**：Agent 处理中（调工具/等待回复）时显示"思考中"转圈 + 已思考秒数；回复开始输出后消失。

**规格**：
1. renderer：聊天区顶部/输入框上方加思考指示条：`⟳ 思考中 12s`（SVG 转圈动画，CSS `@keyframes spin`）
2. 计时：`chatSending = true` 起每秒 +1，显示"已思考 N 秒"；收到首个输出（done/typeInto 开始）→ 清除计时与指示条
3. 工具循环期间持续显示（每轮 tool-start/tool-done 不清零，保持累计——用户看到的是"整体思考时长"）
4. 学科对话同样适用
5. 失败/超时 → 指示条消失并报错

**验收**：发一个需要工具调用的复杂问题 → 看到转圈 + 秒数跳动 → 开始出字后消失。

---

## 三、B. 备忘模块重组（去鸡肋 + 收纳三大件）

### B1. 去掉「老忘记的事儿」
- memoList 相关 UI 下线；**数据保留不删**（workspace.json 里 `类型=备忘` 的记录仍在，仅界面不再展示/入口移除）
- memo 面板只剩亲友画像（familyList），删掉 memo/family 子页切换的 seg 结构

### B2. 收纳：技能库 + 学习词典 + Agent 记忆 从设置移入备忘模块
- 备忘面板改为 **Tabs**：`亲友画像 | 技能 | 学习词典 | 记忆`（新名字见 B3）
- 各子页直接搬设置里的现成实现：
  - 技能列表/编辑/删除/禁用（现 renderer 记忆面板的 skills 区）
  - 学习词典计数+清空（现 panel-parse）
  - 记忆事实列表 + 来源跳回 + 删除（现 panel-memory 的 facts 区）
- 设置里对应入口**移除**（设置 → 记忆 / 设置 → 智能解析 两面板删除或留"已移至备忘"占位）

### B3. 备忘模块改名（**名字未定，用户后续指定**）
- 实现方式：把模块显示名收敛为一个常量（如 `MODULE_MEMO_NAME`），所有出现处（导航 tab、标题、快捷键提示、关于页）读常量
- 默认占位名先用「备忘」，用户定名后只改常量一处
- 候选（用户参考，不必现在定）：亲友簿 / 人际 / 珍藏 / 记事本

**验收**：备忘面板 = 新名 + 4 个 Tabs 都能正常用；设置里不再有记忆/智能解析入口；老忘记的事儿界面消失但旧数据文件未删。

---

## 四、C. 今日 + Agent 合并为「主页」

### C1. 合并
- 导航变为 4 Tab：**主页 / 日程 / 备忘(新名) / 学习**（Agent 模块融入主页，导航里删 agent tab）
- 快捷键映射调整（原 1-5 → 1 主页/2 日程/3 备忘/4 学习；设置-快捷键面板同步文案）

### C2. 主页布局（用户设计）
```
┌ 主页 ─────────────────────────────┐
│ 仪表盘（缩略版，一行紧凑 KPI 卡）    │  ← 原今日概览的浓缩：逾期数/今日待办/近一周/生日
│   （鼠标 hover/点击某卡 → 该卡放大  │     展示详情 —— 桌面端 hover 放大 + 点击置顶展开）
├──────────────────────────────────┤
│ Agent 对话（原 Agent 模块整块）     │  ← 占页面主要高度
└──────────────────────────────────┘
```
- 顶部仪表盘做成 **可折叠**（点小箭头收成一条，给 Agent 更多空间），状态记忆（localStorage）
- 卡片 hover 放大实现参考现有 today-card 的 hover scale 效果；点击某卡 → 弹详情或下方展开
- 原「今日」模块的四象限/详情内容放入仪表盘放大的卡片内（或保留一个「今日详情」按钮打开原今日面板 modal）
- **今日面板的完整内容（四象限表格/时间轴入口）不丢**——收进缩略卡展开视图

**验收**：导航 4 Tab；主页上方缩略仪表 + 下方 Agent 对话；hover/点击卡片可放大看详情；原今日功能从主页可达。

---

## 五、D. 聊天记录体系（日期 → 会话 树）

### D1. 主 Agent 落库
- 主 Agent 对话从纯内存改为**落 conversations.db**：主 Agent 每天一个会话（或每连续对话段一会话）
- 会话 `subject='主Agent'`，`title=日期`（如 `2026-09-02`），新建时查当天是否已有主 Agent 会话 → 有则续用（当天一个对话流，跨天自动新会话）
- 学科会话保持不变（subject=学科名），与主 Agent **天然区分**（conv_id 隔离，无需改）

### D2. 聊天记录查看器（会话树）
- 新 UI（放主页/设置？建议放主页 Agent 区顶部「历史」按钮 或 设置新增「聊天记录」）：
```
▸ 2026-09-02（周三）
    ├─ 主 Agent    14 条
    ├─ 数学        8 条      ← 学科会话归到它们发生的日期
    └─ 英语        3 条
▸ 2026-09-01
    └─ 主 Agent    5 条
```
- 点击会话 → 打开该会话对话（只读查看或可继续聊）
- 实现：conversations.db 已有 created_at/updated_at；按日期分组查询 + subject 展示即可，**无需改表结构**（date(subject 主 Agent 新增区分)；可按 updated_at 的日期归类）

### D3. 日程模块跳转入口
- 日程（schedule）模块日视图/详情区加按钮「📅 当日 Agent 记录」→ 跳到该日的 Agent 聊天记录（打开 D2 的查看器定位到该日主 Agent 会话）
- 无记录时按钮置灰/提示"当日无对话"

**验收**：主 Agent 对话落库（重启可找回当天历史）；查看器按"日期 → 主/学科"树展示；日程某天有对话时可一键跳到那天的记录。

---

## 六、明确不做（防蔓延）

- ❌ 语音输入（Electron 无 Web Speech 识别，需云/本地 ASR 另行决策）
- ❌ 周期性任务（每周三重复事件——单独排期）
- ❌ 真流式 Agent（工具循环重构——单独排期）
- ❌ 仪表盘大改视觉（沿用现有风格只做布局合并）
- ❌ 数据表结构变更（conversations.db 不加新表；老忘记的事儿数据不动）

## 七、技术约束

- 纯 JS 零新 npm 依赖；SQLite 用 node:sqlite（已有）；代码/注释英文、UI 中文
- 模块显示名（备忘新名）收敛为**一个常量**便于改名
- 主 Agent 落库复用 conv.js 现成会话 API（createConversation/list/open/append），subject='主Agent'——**不要新开一套存储**
- 删除任何 UI 前先确认对应数据逻辑不删（数据保留原则）

## 八、验收清单（全部满足 = 完成）

1. 思考转圈 + 秒数显示 ✅
2. 备忘面板 = 新名(占位) + 亲友画像/技能/学习词典/记忆 4 Tabs ✅；设置里旧入口移除 ✅；老忘记的事儿界面下线、数据未删 ✅
3. 导航 4 Tab（主页/日程/备忘/学习）✅；主页 = 缩略仪表(可折叠) + Agent ✅；原今日功能从主页可达 ✅
4. 主 Agent 对话按天落库（当天续聊、跨天新会话）✅
5. 聊天记录查看器「日期 → 主/学科」树，点击打开 ✅
6. 日程有「当日 Agent 记录」跳转按钮 ✅
7. 既有功能回归：学科 AI/解析/detect/记忆自动存/打字机/操作条 不受影响 ✅
8. 快捷键与设置文案随模块变化同步 ✅
9. 交接文档交付（结构改动图、新 UI 说明、遇到坑、代码清单）✅

## 九、开工前复述（九问，全对再动手）

1. 备忘模块删了"老忘记的事儿"，它的数据怎么办？
2. 技能/学习词典/记忆从哪搬到哪？设置里原来的入口怎么处理？
3. 备忘模块新名定了吗？实现上怎么让它"一处改名全局生效"？
4. 导航合并后剩哪几个 Tab？原"今日"的完整功能去哪了？
5. 主 Agent 对话存到哪？用什么区分主 Agent 和学科会话？
6. 聊天记录树按什么分组？要改数据库表结构吗？
7. 日程里"当日记录"按钮跳去哪、没记录怎么办？
8. 本期明确不做哪五件事？
9. 有哪些数据是"界面下线但文件保留"的？

## 十、执行顺序

1. 读本文件 + 读 renderer 相关段（导航/备忘/今日/agent/设置记忆面板）+ conv.js
2. A 思考动画（最小改动热身）
3. D 落库 + 会话树 + 日程跳转（动 conv 集成，先做——B/C 的移动都依赖结构稳定）
4. B 备忘重组（移 UI + 改名常量 + 收 Tab）
5. C 主页合并（导航改 4 Tab + 仪表缩略 + 布局）
6. 快捷键/设置文案同步
7. 全量回归 + 验收 9 项
8. 写交接文档（必交）→ 提交推送（`feat(1.1):` 前缀）

## 十一、环境坑（沿用）

- Electron 直接跑：`env -u ELECTRON_RUN_AS_NODE NODE_OPTIONS= ./node_modules/.bin/electron . --no-sandbox --disable-gpu --disable-gpu-compositing`
- 测试脚本勿硬编码 API Key（`process.env.JW_TEST_KEY`）；用完删除
- renderer 不支持 window.prompt/confirm（用内联输入 + modal）
- git push 网络波动重试 / `-c http.proxy=` 绕过

*交接日期 2026-09-02 · 执行模型建议 Kimi K3（用户指定）*
