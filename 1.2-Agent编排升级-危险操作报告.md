# 1.2 Agent 编排升级 — 危险操作报告

> 对应需求：`1.2-Agent编排升级-对接文档.md` 第五节（安全边界与危险操作报告，必交）
> 执行方：WorkBuddy
> 日期：2026-09-03
> 授权模式：用户已授权「完全权限免打断执行」（范围以第五节为限）

---

## 一、结论（先给结论）

**本次执行零数据删除、零工作区外改动、零不可逆操作。** 全部改动为 `D:\Jovan's Workplace\` 内 6 个源码文件的**新增/修改**，未删除任何文件、未触碰任何数据库既有数据、未执行任何 `rm`/`del`/清库操作。

唯一的「危险能力」是**新增**了会话删除功能代码（`deleteConversation`），该功能为**物理删除**，已内置「两次点击确认」防护，本次执行中**未实际触发**任何删除。

---

## 二、工作范围自证（对照 5.1 / 5.3 / 5.4）

| 检查项 | 结果 |
|--------|------|
| 可写路径唯一 = `D:\Jovan's Workplace\` | ✅ 全程仅在此目录内操作 |
| 工作区外（桌面/下载/文档/系统目录） | ✅ 零改动（`git status` 仅列出 `app/` 下 6 文件） |
| 删除数据文件（conversations.db / data 等） | ✅ 未删除、未清空、未迁移 |
| 修改表结构 / 迁移数据 | ✅ 未改表结构（仅新增删除函数，无 DDL 变更） |
| 新增 npm 依赖 / 全局安装 | ✅ 零新增依赖 |
| 硬编码 API Key / 泄露凭证 | ✅ 未硬编码（测试未用真实 key） |

---

## 三、改动文件清单（全部为「修改」，无「删除」）

| 文件 | 操作 | 说明 |
|------|------|------|
| `app/ai.js` | 修改（+114） | 新增 ask_user/plan 桥接、fallbackModel、runAgentLoop 改造 |
| `app/main.js` | 修改（+52） | 新增 shouldPlan、ai:answer/ai:plan-answer/conv:delete IPC |
| `app/preload.js` | 修改（+5） | 暴露 aiAnswer/aiPlanAnswer/convDelete |
| `app/conv.js` | 修改（+14） | 新增 deleteConversation 函数 + 导出 |
| `app/tools.js` | 修改（+5） | TOOL_DEFS 新增 ask_user 工具定义 |
| `app/renderer/index.html` | 修改（+269） | Agent dock、hover、ask/plan 卡、收尾建议、会话树按钮 |

**合计：6 文件，+439 / -20 行。无文件被删除、移动或重命名。**

---

## 四、危险操作明细（唯一一项，如实记录）

### 新增「会话删除」能力（代码级，非本次实际删除）

- **位置**：`app/conv.js#deleteConversation(id)` + `main.js conv:delete` + `renderer hisDelete()`
- **性质**：**物理删除**——删除 `conversations` 表对应行、其 `messages` 行、`messages_fts` 索引行；并将引用该会话的 `facts.source_conv` 置 `NULL`（**保留事实本身，仅摘除来源回链**）。
- **防护措施**（已内置）：
  1. 前端 `hisDelete()` 采用**两次点击确认**（第一次 `toast` 提示「再次点击删除按钮确认（不可恢复）」+ 按钮变「确认」红底高亮，第二次才真正调 `convDelete`）。
  2. 沿用项目既有模式（`subjectClear` 的 arm 二次确认），与 `1.1` 删除交互一致。
- **本次是否触发**：❌ 未触发（仅代码落地 + 语法校验，未运行应用、未删除任何会话）。

### 关于 facts 的软删除语义说明

`deleteConversation` 删除会话时**不删除** fact 记忆（`facts` 表不动，仅 `source_conv` 置空）。这是刻意的数据保留设计：记忆事实是用户长期资产，即使来源会话被删也不应连带删除。若后续要「级联删除来源事实」，需单独决策。

---

## 五、其他说明

1. **语法校验**：`node --check` 覆盖 ai/main/preload/conv/tools/data/skills/parse 全部通过；`node check-html.js` 返回 `failures: 0`。
2. **临时脚本**：本次未创建一次性冒烟脚本（纯静态语法校验 + 代码审查），无残留文件。
3. **未触碰**：`.workbuddy` 目录、`conversations.db`、`data` 数据文件、git 历史（尚未提交，见交接文档后续步骤）。

---

*本报告与 `1.2-Agent编排升级-交接文档.md` 配套交付。*
