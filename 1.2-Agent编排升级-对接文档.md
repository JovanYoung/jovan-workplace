# Jovan's Workplace · 1.2 最终全量交接（主页 UI 精修 + Agent 编排升级 + 工程项）

> 唯一交接文档 · v2 定稿 2026-09-03 · 承接 1.1（已完成 062b7d7）
> **本次用户已授权"完全权限免打断执行"——但必须严格遵守第五节安全边界，且交付时必须附危险操作报告。**
> 代码基址：`D:\Jovan's Workplace\app`（git main）；参考：`1.1-大改版-交接文档.md`、`AI功能蓝图.md`、`dsh-借鉴清单.md`

---

## 〇、执行总览（本期的全部工作，三大块 + 安全条款）

```
A. 主页 UI 精修（Agent 悬浮窗 + 仪表卡 hover 放大）   ← 用户新点名
B. Agent 编排升级（ask_user / plan-then-act / 收尾建议）← 治"笨"
C. 工程项（前缀缓存布局 + Provider fallback）
D. 顺手收尾（会话树重命名/删除 等备注项，见 B 文档）
⚠️ 第五节：安全边界与危险操作报告（必读必交）
```

> 非本期：MCP / 网页搜索 / 定时任务 / 周期性任务 / 语音输入 / 真流式重构 / subagent —— 原因见第六节（需外部 key 或大重构或无交互易跑偏，已排除）。

---

## 一、A. 主页 UI 精修（用户点名，未实现项）

### A1. Agent 悬浮中下窗（像导航栏一样可收起/展开）
现状：1.1 主页 Agent 是页面内普通区块。改为**悬浮 dock**：
- 悬浮于主页**中下方**（bottom 中央区域），类似底部 dock / 导航条风格：圆角悬浮层、毛玻璃、投影、顶部一条"把手"
- 把手操作：
  - **点击收起**（下拉）：Agent 窗收起为**一条细栏**（仅剩输入框/或仅剩图标条），给仪表盘让位
  - **再次点击展开**：恢复为完整对话区
  - 收起/展开状态存 `localStorage`（键 `wb_agent_dock`），刷新保持
- 占领面积：先做固定比例（约视口高 45-55%），后续用户微调——**高度做成可上下拖拽调节**（把手拖拽改变高度，存 localStorage `wb_agent_dock_h`），拖不动也要留出 CSS 变量便于后续改
- 与主页其他内容关系：悬浮层覆盖在内容区之上（z-index 高于仪表区）；仪表盘区域在 Agent 收起时完整可见
- 样式参照现有导航条（毛玻璃 + 圆角 + 主题色），不引入新风格

### A2. 仪表卡 hover 放大（1.1 只做了点击展开，补 hover）
- 四张缩略卡 `.today-card`：**鼠标 hover 到某卡 → 该卡放大**（scale + 阴影 + 上浮），放大后直接展示该卡摘要详情（无需点击）
- 移出 → 恢复；点击 → 保持展开（手风琴，现状保留）
- 实现注意：hover 放大避免遮挡相邻卡片交互（放大用 transform + 高 z-index）；触屏无 hover 则保留点击展开兜底

### A3.（衔接）主页滚动/布局微调
- 若悬浮 dock 与仪表盘重叠影响布局，允许把主页内容改为可滚动区，dock 固定悬浮（position: fixed / sticky 区域）

---

## 二、B. Agent 编排升级（治"笨"，原 1.2 主任务不变）

### Step 1：ask_user 工具（主动澄清）
- [ ] tools.js 新增 `ask_user`（参数：question + 可选 options 数组，1-2 个问题）
- [ ] runAgentLoop：模型调 ask_user → 暂停 → `onEvent({type:'ask', question, options})` → renderer 弹**问题卡**（候选按钮 + 自由输入）→ 用户答 → 回答回填 `{role:'user'}` 继续循环
- [ ] system prompt 引导："目标模糊/缺关键信息/多种合理解读时，先 ask_user，不要瞎猜"
- [ ] 防滥用：每任务 ask_user ≤3 次；能查数据解决的不准问

### Step 2：plan-then-act（复杂委托先出计划）
- [ ] 双模式：默认快速（现状）；检测"复杂委托"（关键词"帮我/安排/规划/整理/做一份/比较/分析"等 或 预估 ≥3 步）→ 先非执行性请求出 `{plan:[...]}` → `onEvent({type:'plan'})` → renderer 弹**计划卡**（[执行][修改]）→ 确认后才跑工具循环
- [ ] 日常问答绝不触发计划模式

### Step 3：任务收尾主动建议
- [ ] 完成写操作/生成内容后，flash 检测 → 弹轻量建议（可忽略）：「已添加 ✅ 要我提醒吗？」「要不要存成技能？」
- [ ] 与记忆/技能卡去重（见 1.2 文档 Step3 细则）

### Step 4：思考条与 ask/plan 卡衔接（1.1 thinkBar 复用）

> 详细规格以 v1 的 `1.2-Agent编排升级-对接文档.md` Step1-4 为准，此处只列要点；执行时合并阅读。

## 三、C. 工程项

### C1 前缀缓存友好布局：固定 base+tools+历史在前，动态注入（记忆/资料/今日数据）移到 system 尾部或独立消息，保证多轮前缀字节级稳定（省钱 1/30）；交验证对比
### C2 Provider fallback：401/429/5xx/网络错 → 自动切同厂商备用模型或下一已配置厂商 → 重试 1 次 → 才报错；renderer 提示"已切换 X 重试"

## 四、D. 顺手收尾（视进度，低优先）
- 会话树（#historyMask）加：会话重命名 / 删除（复用 conv.js 已有 API，节点上加按钮）
- 备忘模块定名占位不变（MODULE_MEMO_NAME 常量机制已就绪，等用户定名）

---

## 五、⚠️ 安全边界与危险操作报告（必读必交）

### 5.1 工作范围唯一
- **可写路径仅限：`D:\Jovan's Workplace\`**（app/ 源码 + data/ 数据 + dist 产物 + 根目录文档）
- **绝对禁止**改动/删除/创建 `D:\Jovan's Workplace\` 之外的任何内容：
  `C:\` 用户目录、`D:\` 其他项目（含 dsh-bridge、训练项目等）、WorkBuddy 安装目录、系统目录
- 如需读取工作区外文件做参考 → **只读**，不得写

### 5.2 删除纪律
- 删除前先 `git status` 确认该文件属于本仓库且改动已知
- 源码删除 = 用 git 提交历史兜底；临时测试文件删除前确认非用户数据
- **data/ 用户数据（workspace.json/conversations.db/skills/）非经确认不得删**——只允许测试产生的临时数据

### 5.3 允许的自主行为（无需每次确认，但须记入报告）
- npm 包安装到 `app/node_modules`（如需，标注是否真的需要）
- 下载渲染资源/图标到 app/assets
- 本地起服务/跑 electron 测试（用完关闭）
- git 提交/推送（仅本仓库 main）

### 5.4 禁止行为
- `npm install -g`、修改系统环境变量、注册表、服务
- 杀非自启进程、修改其他应用文件
- 推送非本仓库的 git 操作、改 remote

### 5.5 交付必须附【危险操作报告】
报告列出：① 删除/覆盖了哪些文件（含临时文件）② 安装了哪些 npm 包/下载了哪些资源 ③ 是否动过工作区外任何路径（应为"无"）④ git 提交清单 ⑤ 数据文件是否有变更。用户要"有数"。

---

## 六、明确不做（本期排除 + 原因）

| 项 | 排除原因 |
|---|---|
| MCP client | 需用户选外部 server，属"造接口大期"，且无交互易选错方向 |
| 网页搜索工具 | 依赖 key 决策（Tavily/自建），需用户定 |
| 定时任务/后台 agents | 编排未稳前不宜叠加 |
| 周期性任务（recurrence）| 数据模型大改，必须用户在场确认迁移规则 |
| 语音输入 | ASR 方案未定 |
| 真流式 Agent | runAgentLoop 流式化重构风险高，无交互易翻车 |
| subagent 多代理 | 等委派需求 |

## 七、验收清单（A 6 项 + B 6 项 + C 2 项 + 安全 1 项 = 15 项全过 + 交接 = 完成）

A1 主页 Agent 悬浮于中下（可收起为细栏/再展开、状态记忆）✅
A2 高度可拖拽调（或留 CSS 变量）✅
A3 hover 卡片放大即显详情；点击保持展开 ✅
A4 ask_user：模糊需求弹问题卡（选项+自由输入）→ 回答后续答 ✅
A5 复杂委托先弹计划卡 → 确认才执行 ✅
A6 任务收尾轻建议（不与记忆/技能卡重复）✅
B1 ask_user ≤3 次、简单问题不问 ✅
B2 日常问答不触发计划模式 ✅
B3 思考条在 ask/plan 期间状态正确 ✅
B4 缓存前缀验证：2 轮工具循环请求前缀一致 ✅
B5 fallback：key 失效/429 → 自动切换重试 + 提示 ✅
B6 1.1 全功能回归（主页/备忘/学习/日程/会话树/操作条/打字机）✅
C1 工作区外零改动（交接自证）✅
C2 会话树重命名/删除（若 D 做了）✅
C3 语法检查 0 failures ✅
D 【危险操作报告】随交接交付 ✅

## 八、执行顺序

1. 合并读 v1（1.2 编排细则）+ v2（本文件 UI/安全）+ 1.1 交接（了解现状结构）
2. A 主页 UI（A1 悬浮窗 → A2 hover 放大 → A3 布局衔接）——独立可测
3. B 编排（Step1 ask_user → Step2 plan → Step3 收尾 → Step4 思考衔接）
4. C1 缓存布局 → C2 fallback
5. D 顺手项（会话树按钮，时间够才做）
6. 全量回归 + 验收 15 项
7. 写交接文档（含【危险操作报告】必交）→ `feat(1.2):` 提交推送

## 九、环境坑（沿用）

- Electron 直跑加 `--no-sandbox --disable-gpu --disable-gpu-compositing`；env -u ELECTRON_RUN_AS_NODE + NODE_OPTIONS=
- **带 BrowserWindow 的冒烟脚本 stdout 会静默** → 断言写文件（1.1 交接坑 4）
- 测试脚本禁硬编码 API Key（env JW_TEST_KEY）；renderer 禁 window.prompt/confirm
- git push 波动重试 / `-c http.proxy=` 绕过

## 十、开工前复述（八问，全对再动手）

1. A1 悬浮窗收/展交互是什么样？状态存哪？
2. A2 hover 放大与点击展开怎么共存（触屏兜底）？
3. ask_user 什么时候不该问？
4. plan 模式靠什么触发？谁决定？
5. 收尾建议怎么和记忆/技能卡去重？
6. 可写路径唯一是哪个目录？禁止动哪里？
7. 删除文件前要做什么？数据文件能不能删？
8. 交付时除了代码还要交哪两份东西（交接文档 + ?）

*交接日期 2026-09-03 · 用户已授权免打断执行（范围以第五节为限）*
