# Jovan's Workplace · 1.2 开工对接文档（Agent 编排升级）

> 唯一交接文档 · 日期 2026-09-03 · 承接 1.1（大改版，Kimi K3 执行中）
> **开工前先完成第九节「七问复述」并得到用户确认。**
> 代码基址：`D:\Jovan's Workplace\app`（git main 分支）；需求总账：`AI功能蓝图.md`；参考：`dsh-借鉴清单.md`

---

## 〇、背景与定位声明

1.1 把产品从「工作台」推向「AI 助理」（主页=操作台+仪表盘、Agent 为主操作形式）。**1.2 的任务是让主 Agent 变"聪明"**：从"单次问答 + 闷头调工具"升级为"会主动澄清需求、会先给计划再执行、会在对话收尾主动给建议"的编排层升级。参考 dsh 的分阶段 loop / ask_user 模式（见 dsh-借鉴清单 A1-A3）。

## 一、主任务（Agent 编排升级，四步）

### Step 1：ask_user 工具（主动澄清）
- [ ] tools.js 新增工具 `ask_user`（参数：question 字符串，一次 1-2 个问题，可带 options 候选答案）
- [ ] runAgentLoop：模型调用 ask_user → **暂停循环** → 经 onEvent({type:'ask', question}) 推 renderer
- [ ] renderer 弹**问题卡**（inline，仿 draft 卡）：显示问题 + 候选答案按钮 + 自由输入框 → 用户回答 → 回填 `{role:'user', content: 回答}` → 循环继续
- [ ] system prompt 引导："当任务目标模糊、缺关键信息、有多种合理解读时，先调用 ask_user 澄清，不要瞎猜"
- [ ] 防滥用：每轮任务 ask_user **最多 3 次**；简单问题（可查数据的）不得用 ask_user

### Step 2：plan-then-act（计划确认，复杂委托）
- [ ] runAgentLoop 双模式：
  - **快速模式**（默认）：现状行为（直接答/轻量工具）
  - **计划模式**：判断"复杂委托"（需 ≥3 步工具调用 或 用户明说"帮我做…"）→ 先输出计划
- [ ] 计划呈现：工具循环前，模型先生成 `{plan: [步骤1,2,3...]}`（新工具 `propose_plan` 或结构化输出）→ onEvent({type:'plan'}) → renderer 弹**计划卡**：[执行][修改计划(自由输入)] → 用户确认才继续执行
- [ ] 实现要点：plan 通过**非执行性调用**拿（调一次 LLM 只出计划，不跑工具），确认后进入常规工具循环
- [ ] 判断函数放主进程：消息里出现"帮我/安排/规划/整理/做一份/比较/分析"等高复杂度词 或 历史 rounds>3 才走计划模式；日常问答不打扰

### Step 3：对话收尾主动建议（升级 detect）
- [ ] 对话完成后现有 detect/draft 保留；**新增"收尾建议"**：flash 检测是否完成了一项任务（刚写了日程/生成了内容/做完了分析）
- [ ] 完成后弹轻量建议卡（不打扰，2 秒可忽略）：如「已添加日程 ✅ 要我顺便提醒吗？」/「分析完成，要不要存成技能？」
- [ ] 与现有 runMemExtract/maybeDistillSkill 区分触发：收尾建议 = 任务型收尾，记忆/技能 = 事实/流程沉淀，别重复弹

### Step 4：思考显示增强（衔接 1.1 A）
- [ ] ask/plan 卡出现时思考指示条文字变化（"正在向你确认…"）；计时保留

## 二、工程项（顺手做，低成本）

### E1：前缀缓存友好 prompt 布局
- [ ] 现状：buildSystemPrompt = 固定 base + **动态注入**（记忆/学科资料/今日数据）拼接
- [ ] 重构为"前缀稳定"：`[固定 system 基础]` → `[固定工具说明/对话历史]` → **动态注入移到 system 尾部或作为独立 user 前缀消息**
- [ ] 目标：多轮对话/工具循环中，system+tools+已发历史保持字节级稳定 → DeepSeek 前缀缓存命中（输入价 1.5→0.05 元/百万）
- [ ] 验证：抓 2 轮工具循环请求体，确认除最后一轮外前缀一致

### E2：Provider 自动 fallback
- [ ] 主进程 LLM 调用捕获 401/429/5xx/网络错误 → 自动切**同厂商备用模型**（flash→pro 反向不行则切下一已配置厂商）→ 重试 1 次 → 失败才报错
- [ ] renderer 提示"已自动切换 X 模型重试"

## 三、明确不做（backlog，排队）

- ❌ MCP client（造接口一期大，单列）
- ❌ 网页搜索工具（web seam，依赖 MCP 或直连 Tavily，单列）
- ❌ 定时任务/后台 agents（Hermes cron 式）
- ❌ 周期性任务（recurrence 数据模型）
- ❌ 语音输入（需 ASR 方案决策）
- ❌ 真流式 Agent（runAgentLoop 流式化重构）
- ❌ Subagent 多代理（等定时/委派需求出现）

## 四、技术约束

- 纯 JS 零新 npm 依赖；复用 ai.js（runAgentLoop/jsonOutput/chatOnce）、tools.js、conv.js 既有
- ask/plan 卡 UI 仿现有 draft-inline 卡样式（毛玻璃+弹性），风格统一
- 代码/注释英文、UI 中文；不破坏 1.1 的模块结构

## 五、验收标准（全过 = 完成）

1. 说「帮我安排下周学习计划」→ **先弹计划卡**（步骤列表）→ 确认后执行 ✅
2. 说「帮我记个事」这类模糊需求 → Agent **先 ask_user 问 1-2 个关键问题**（如"记到日程还是备忘？什么时候？"）→ 按回答执行 ✅
3. 执行完写日程/任务 → 收尾弹轻量建议（不重复弹记忆/技能卡）✅
4. ask_user 一轮最多 3 次，简单可查问题不问 ✅
5. 工具循环 2 轮请求体前缀一致（缓存友好验证）✅
6. 主模型 key 失效/429 → 自动切备用模型重试成功，有提示 ✅
7. 日常问答不触发计划模式（无打扰）；1.1 功能回归 ✅
8. 交接文档（实测样例：ask/plan 卡触发与回答、缓存前缀对比、fallback 日志、坑、改动清单）✅

## 六、执行顺序

1. 读 dsh-借鉴清单.md A1-A3 + 现有 ai.js（runAgentLoop）/tools.js/renderer（draft-inline 卡/思考条）
2. Step 1 ask_user（工具+事件+问题卡）
3. Step 2 plan-then-act（双模式+计划卡+判断函数）
4. Step 3 收尾建议 + Step 4 思考条衔接
5. E1 前缀布局重构（含验证脚本思路）→ E2 fallback
6. 全量回归 + 验收 8 项
7. 写交接文档（必交）→ 提交（`feat(1.2):` 前缀）

## 七、环境坑（沿用）

- Electron 直跑：`env -u ELECTRON_RUN_AS_NODE NODE_OPTIONS= ./node_modules/.bin/electron . --no-sandbox --disable-gpu --disable-gpu-compositing`
- 测试脚本禁硬编码 Key（`process.env.JW_TEST_KEY`）；renderer 禁 window.prompt/confirm
- git push 波动重试 / `-c http.proxy=` 绕过

## 八、backlog 备注（下次聊再排）

- MCP client 造好后：**用户自助接任何外部工具**（一条配置），JW 进入"VS Code 模式"
- 技能 json 手动编辑：M4 已有查看/编辑/删除 UI，够用；如要"从对话批量提炼技能"再强化
- 产品定位（工作台 → AI 助理）已随 1.1 落地，蓝图待同步 v4

## 九、开工前复述（七问）

1. ask_user 和 plan 卡分别什么时候触发？怎么防滥用/防打扰？
2. 计划模式怎么判断（谁决定走计划模式）？
3. 前缀缓存友好 = prompt 怎么排？动态注入放哪？
4. fallback 切模型的规则是什么？
5. 收尾建议和现有记忆/技能卡怎么避免重复弹？
6. 本期明确不做哪六件事？
7. 完成后除了代码还要交什么？

*交接日期 2026-09-03 · 承接 1.1 之后执行*
