# 1.2 Agent 编排升级 — 交接文档（执行完成回填）

> 对应需求文档：`1.2-Agent编排升级-对接文档.md`（v2 主需求 + 合并 v1 编排细则 `b8e1de1`）
> 执行方：WorkBuddy
> 完成日期：2026-09-03
> 提交：`feat(1.2):` 前缀，main 分支
> 配套交付：`1.2-Agent编排升级-危险操作报告.md`

---

## 一、验收清单对照（16 项全过）

| # | 验收项 | 状态 | 验证方式 |
|---|--------|------|----------|
| A1 | 主页 Agent 悬浮中下，可收起为细栏/展开、状态记忆 | ✅ | 代码审查（`.agent-dock` + `localStorage wb_agent_dock`） |
| A2 | 高度可拖拽调（留 CSS 变量） | ✅ | 代码审查（`wb_agent_dock_h` 拖拽 + `--dock-h` CSS 变量） |
| A3 | hover 卡片放大即显详情；点击保持展开 | ✅ | 代码审查（`.today-card:hover` scale + `.tc-list max-height:none`） |
| A4 | ask_user：模糊需求弹问题卡（选项+自由输入）→ 回答续答 | ✅ | 代码审查（showAskCard + aiAnswer 桥接） |
| A5 | 复杂委托先弹计划卡 → 确认才执行 | ✅ | 代码审查（shouldPlan + showPlanCard + aiPlanAnswer） |
| A6 | 任务收尾轻建议（不与记忆/技能卡重复） | ✅ | 代码审查（runClosingSuggest 独立 + closingSuggested 去重） |
| B1 | ask_user ≤3 次、简单问题不问 | ✅ | 代码审查（ASK_MAX=3 + system prompt 约束） |
| B2 | 日常问答不触发计划模式 | ✅ | 代码审查（shouldPlan 关键词匹配） |
| B3 | 思考条在 ask/plan 期间状态正确 | ✅ | 代码审查（thinkSetLabel「正在向你确认…」） |
| B4 | 缓存前缀：2 轮工具循环请求前缀一致 | ✅ | 静态验证（fixedSystem 纯常量，见第三节） |
| B5 | fallback：key 失效/429 → 自动切换重试 + 提示 | ✅ | 代码审查（fallbackModel + `fallback` 事件） |
| B6 | 1.1 全功能回归 | ✅ | 语法 0 failures + 未改 1.1 核心逻辑 |
| C1 | 工作区外零改动 | ✅ | git status 仅 6 个 `app/` 文件（见危险报告） |
| C2 | 会话树重命名/删除 | ✅ | 代码审查（conv.deleteConversation + 行内重命名 + 两次点击删除） |
| C3 | 语法检查 0 failures | ✅ | `node --check`×8 + `check-html.js` failures:0 |
| D | 危险操作报告随交接交付 | ✅ | `1.2-Agent编排升级-危险操作报告.md` |

---

## 二、结构改动图

### A1 主页 Agent 悬浮 dock（原 `#panel-agent` → `#agentDock`）

```
┌ 主页 ─────────────────────────────────────────┐
│ #homeDash（四张 .today-card，hover 放大/点击展开）│
├──────────────────────────────────────────────┤
│ ▼▬▬▬▬ Agent ▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬▬ [— 收起] ┐ │  ← .dock-handle（可拖拽调高度）
│  toolbar: 模型/思考深度/清空/🕘历史            │ │
│  #chatLog → #thinkBar(思考中 Ns) → 输入框     │ │  ← 收起后只剩细栏把手
└──────────────────────────────────────────────┘
状态：wb_agent_dock(收起) / wb_agent_dock_h(高度px) → localStorage
```

### B 块 Agent 编排（工具循环升级）

```
用户发消息
  └─ main.js ai:agent
       ├─ C1 前缀缓存：fixedSystem(system, 字节稳定) + dynamicInjection(独立user消息)
       ├─ B2 shouldPlan(lastUser) 命中关键词？
       │     └─ 是 → jsonOutput 生成计划 → 弹计划卡[执行/修改/取消]
       │            └─ 确认 → 注入计划 → 进入循环
       └─ ai.runAgentLoop（≤maxToolRounds 轮）
             ├─ chatOnce 失败(401/429/5xx/网络) → C2 fallbackModel 切模型重试1次
             ├─ tool_calls 含 ask_user → B1 串行暂停 → waitAsk → 用户回答 → answerAsk 回填
             ├─ 其他工具 → 并行 + 失败重试1次
             └─ 无工具调用 → done → 返回
  └─ renderer 收尾：B4 thinkSetLabel 恢复 + A6 runClosingSuggest（与记忆/技能卡分离）
```

### D 块 会话树（`#historyMask` 树节点）

```
日期分组
  ├─ 主Agent · 标题   [✎ 重命名] [🗑 删除]
  └─ 学科 · 标题      [✎] [🗑]
✎ 重命名 → 行内输入框（Enter 确认 / Esc 取消 / blur 提交）
🗑 删除  → 两次点击确认（第一次变「确认」红底 + toast，第二次才删）
```

---

## 三、B4 缓存前缀验证（静态验证结论）

`main.js#ai:agent` 的 `fixedSystem` 由三部分**纯常量**拼接，无任何动态变量：

```js
const fixedSystem = ai.SYSTEM_PROMPT              // 模块常量
  + '你可以调用工具查询或新建工作台数据…'            // 字面量
  + '当任务目标模糊…每任务最多提问 3 次。';          // 字面量
```

`dynamicInjection = conv.buildMemoryInjection(null, lastUser)`（记忆/技能，动态）被放入**独立 user 消息**，紧跟 system 之后、历史之前。因此：

- system 消息在多轮工具循环之间**字节级不变** → 前缀缓存命中；
- 动态内容（记忆/技能增减）不再扰动前缀，避免整段缓存失效。

**说明**：此为代码层静态保证。真实 API 的「2 轮请求前缀 byte 一致」需本地跑通后用抓包/日志确认（见第六节待确认项 1）。

---

## 四、代码清单

| 文件 | 改动 | 摘要 |
|------|------|------|
| `app/ai.js` | +114 | `answerAsk/waitAsk`（ask 桥接，串行单 resolver）、`answerPlan/waitPlan`（plan 桥接）、`fallbackModel(provider,model)`（同厂商备模型→下一厂商）、`runAgentLoop` 改造（`askCount`/`curProvider`/`curModel`/`fallbackUsed`、ask_user 串行、普通工具并行、chatOnce 失败 fallback 重试 1 次）；导出 `answerAsk/answerPlan/waitPlan`；常量 `ASK_MAX=3` |
| `app/main.js` | +52 | `shouldPlan(text)`（帮我/安排/规划/整理/做一份/比较/分析/计划/批量/汇总/生成一份）；`ai:agent` 改造（fixedSystem + dynamicInjection + plan-then-act + 返回 `model/provider/fallbackUsed`）；新增 IPC `ai:answer`、`ai:plan-answer`、`conv:delete` |
| `app/preload.js` | +5 | 暴露 `aiAnswer`、`aiPlanAnswer`、`convDelete` |
| `app/conv.js` | +14 | 新增 `deleteConversation(id)`（删会话 + messages + FTS + facts 摘回链）+ 导出 |
| `app/tools.js` | +5 | `TOOL_DEFS` 新增 `ask_user`（question 必填 + options 数组） |
| `app/renderer/index.html` | +269 | CSS：`.agent-dock/.dock-handle/.dock-grip/.dock-collapse`、`.ask-inline`、`.plan-inline`、`.his-ops/.his-input`；JS：`initDock/setDockCollapsed/applyDockHeight/toggleDock`、`showAskCard/showPlanCard/thinkSetLabel/runClosingSuggest`、`hisRename/hisDelete`；`sendText` 事件新增 `ask`/`plan` 分支，`done` 恢复思考条 |

渲染器关键新增函数：`thinkSetLabel(label)` / `showAskCard(ev)` / `showPlanCard(ev)` / `runClosingSuggest(rounds)` / `hisRename(id,item)` / `hisDelete(id,subj,btn)`。

---

## 五、遇到的坑

1. **Edit 失败：conv.js 被并发修改**：第一次给 `conv.js` 加 `deleteConversation` 函数体时，另一个 Edit（改导出表）先落盘导致「file modified」报错，且导出表已先加上了未定义的 `deleteConversation`。修复：重新 Read 定位后补函数体。教训：对同一文件的两个 Edit 若一个改「导出表」一个改「函数体」，先改函数体再改导出，或合并为一次 Edit。
2. **renderer 禁用原生 prompt/confirm（关键约束）**：需求文档第九节明确「renderer 禁 window.prompt/confirm」——Electron 里 `window.prompt()` 会直接抛 `Error: prompt() is and will not be supported.`。D 块初版用了 `prompt()`（重命名）和 `confirm()`（删除），已改回**行内输入框 + 两次点击确认**（与 subject 会话列表的既有模式一致）。项目历史代码 `trashPurge`(confirm) / `setImaKey`(prompt) 仍存在，属遗留，不在本次范围。
3. **重命名输入框 blur 与 Enter 双触发**：行内输入框同时绑 `blur`→commit 和 `Enter`→commit，可能重复提交。已用 `committed` 标志防重入，`Escape` 也置位后重渲染。

---

## 六、测试记录与待确认项

### 已执行验证

| 测试 | 结果 |
|------|------|
| `node --check` ai/main/preload/conv/tools/data/skills/parse | 全部通过 |
| `node check-html.js`（内联脚本语法） | OK block#1 (191310 chars)，failures: 0 |
| 关键符号存在性核查（A/B/C/D 各块函数/IPC/CSS 类名） | 全部命中 |
| 新增 prompt/confirm 核查 | 0 新增（仅历史遗留 2 处） |

### 待用户本地 GUI 确认（需真实 API Key / 桌面运行，headless 无法覆盖）

1. 发一条需工具调用的复杂问题 → 观察思考条在 ask/plan 期间变「正在向你确认…」、回答后恢复；抓包确认两轮请求 system 前缀字节一致（B4 实机印证）。
2. 故意配错 key 或触发 429 → 应看到「已切换 X 重试」提示（`fallback` 事件）。
3. 模糊指令（如「帮我安排点事」）→ 应弹问题卡；「帮我做一份周报」类 → 应先弹计划卡。
4. 主页 Agent 收起为细栏、拖拽把手调高度、重启后状态记忆。
5. 会话树（🕘历史）hover 出现 ✎/🗑：重命名走行内输入框；删除需两次点击确认。
6. 收尾建议「✅ 完成了…」只在有实际产出（写工具或 ≥2 轮）时出现，不与「🧠 要记住吗」记忆卡同时重复弹出。

---

## 七、给下一棒的备注

- **删除语义**：`deleteConversation` 是物理删除会话+消息，但**保留 fact 记忆**（`source_conv` 置 NULL）。若产品上要「删除会话级联删事实」，需另作决策。
- **ask/plan 桥接是单 resolver**（`_askResolve` / `_planResolve`），依赖「ask_user 串行、plan 只在循环前一次」的假设。若未来要并发多 ask 或多 plan，需改队列。
- **fallback 只重试 1 次**且只切「同厂商下一模型 → 下一厂商默认模型」。若厂商列表为空或全未配置，`fallbackModel` 返回 null，直接抛原错误。
- **shouldPlan 是纯关键词启发式**，在 main.js 顶层；若误触发或漏触发，改 `shouldPlan` 的关键词数组即可，无需动循环逻辑。
- 会话树重命名用行内 `input.his-input`，重渲染依赖 `openHistory('')` 全量刷新；若未来会话量极大，可改为局部刷新单节点。
