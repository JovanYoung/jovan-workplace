# Jovan's Workplace 桌面端封装 · 开工指令

> 本文件是**唯一的交接文档**：所有架构决策已定稿，执行者无需询问历史背景。
> 开工前必须先完成第六节的"五问复述"并得到用户确认，再动手写代码。

---

## 一、项目背景

- **产品**：Jovan's Workplace —— 个人效率工作台（当前为单文件网页应用，功能已完整）：
  - 今日四象限（紧急且重要/紧急不重要/重要不紧急/都不重要）
  - 日程（日视图/周视图/日历/列表 四种视图 + 0-24 时间轴 + 事件详情 + 时段编辑 + 未设置时段事件）
  - 备忘（"老忘记的事儿"）+ 亲友画像（忌口/雷点/喜好/生日开关）
  - 学习管理（校内：学期→科目→笔记/课件 tut·lect；校外备考；课件翻译/提问）
  - 一句话智能添加（自然语言解析：时间/地点/优先级自动识别 + 学习词典 + AI 深度解析兜底）
  - Agent 模块、设置（主题三档/导航四位置/内容宽度两档/快捷键全自定义/数据管理/操作日志/回收站/学习词典）
  - 视觉层：深色主题、毛玻璃、水波涟漪、卡片放大、徽标弹跳、环形进度填充等
- **源文件**：`C:\Users\17169\WorkBuddy\2026-08-26-15-04-56\workspace-build\apply-grad-dashboard.html`
  （约 413KB 单文件，CSS/JS/内联 base64 logo 全在里面）
- **数据现状**：页面通过本地桥 `127.0.0.1:8787` 读写 `D:\dsh-data\workspace.json`
  （JSON 格式：`rows` 数组 + birthdays 等，单文件），另有资料库云端 SDK 同步逻辑
- **输出目录**：`D:\Jovan's Workplace`（已建好，空）
- **图标**：`D:\Jovan's Workplace\icon.png`（或 `workspace-build/ledger-ui/jovan-logo2.png`）——
  人工裁剪、白底已转透明、横长比例约 1.72，直接用作应用图标即可

## 二、封装目标（方案 B：下载器 + 主程序分离）

### 产物 1：下载器 EXE（bootstrapper，轻量 2-5MB）
三步引导向导，全中文、风格对齐页面高级感（橙色 #f97316 主色 + 米色底 + 玻璃拟态）：

1. **第 1 步 · 数据位置**：系统目录选择对话框让用户选数据存储文件夹
   - 默认值 `D:\Jovan's Workplace\data`
   - 若检测到旧数据 `D:\dsh-data\workspace.json` 存在 → 提示「检测到旧数据，一键接管？」
2. **第 2 步 · 云端备份（ima）**：勾选框「启用 ima 云端备份」，**默认不勾选**
   - 勾选后显示输入框：ima API Key（密码态显示）
3. **第 3 步 · 确认**：显示「下载主程序（约 XX MB）」+ 「创建桌面快捷方式」勾选（默认勾选）+ [开始安装] 按钮

确认后流程：下载主程序绿色 zip → **SHA256 校验**（与内置哈希比对，不一致即失败提示）→
解压到安装目录（用户级 `%LOCALAPPDATA%\Jovan's Workplace` 风格，**不需要管理员权限**）→
创建桌面快捷方式（按勾选）→ 启动主程序。

### 产物 2：主程序（Electron 应用，绿色 zip 解压即用）
加载改造后的页面，主进程直接管理数据。

## 三、已定稿决策（必须遵守，不得自行更改）

| # | 决策 | 要求 |
|---|------|------|
| 1 | **技术栈** | Electron。主进程 Node.js 直接 fs 读写数据文件，contextBridge 暴露 API 给页面。现有 HTML 改造复用，**不重写 UI** |
| 2 | **数据层** | 主进程读写用户选择的目录下 `workspace.json`，**格式与现有完全一致**（现有数据无缝迁移）。**原子写入**（先写 `.tmp` 再 rename 覆盖）。自动快照。每日自动备份保留最近 **30 份** |
| 3 | **单实例锁** | 二次启动只聚焦已有窗口，绝不两个实例同时写文件 |
| 4 | **ima 备份** | API Key 用 Electron `safeStorage`（Windows DPAPI）**加密落盘**；**key 只存本地、绝不随任何网络请求发送**。备份逻辑照搬现有页面 HTTP 调用。设置里保留「重新填写 ima Key」入口（换机后旧 key 解不开） |
| 5 | **下载安全** | 主程序下载后做 **SHA256 校验**，与内置哈希比对；下载源自管 HTTPS |
| 6 | **签名** | 不做代码签名，接受 SmartScreen「未知发布者」弹窗（自用场景） |
| 7 | **分发形态** | **绿色 zip**（解压即用、不写注册表、删文件夹即卸载），不做 NSIS 安装器 |
| 8 | **窗口行为** | 关闭窗口 = **最小化到托盘常驻**（为系统通知做准备），托盘菜单含「打开/退出」 |
| 9 | **版本号** | 主程序 v0.0.0 起步；正式 GitHub 发行时再升 1.0.0。下载器内置版本检查逻辑 |
| 10 | **页面改造** | ① localStorage 设置（主题/导航位置/快捷键/学习词典）：首启重置默认即可，可加「导入旧设置」可选；② **清掉所有资料库云端 SDK 调用**，数据读写统一走 contextBridge（load/save/backup 一个入口，避免漏改静默失效）；③ 导出/导入改用 `dialog.showSaveDialog` / `showOpenDialog`；④ 系统通知走 Electron Notification |
| 11 | **回归测试** | 封装后过 7 项：数据增删改查、回收站、操作日志、备份、导出导入、学习模块、Agent |

## 四、执行顺序（先骨架后细节，每步能跑再继续）

1. 初始化 Electron 项目（package.json + electron + electron-builder），搭主进程骨架（窗口/单实例锁/托盘）
2. 实现数据 API（fs 读写 / 原子写入 / 快照 / 备份 30 份轮换）+ contextBridge
3. 改造页面（替换 SDK 调用、导出导入对话框、localStorage 迁移、通知）
4. 下载器独立小项目：三步引导 UI + 下载/校验/解压/快捷方式
5. electron-builder 打包配置（图标、asar、绿色 zip 目标）
6. 端到端跑通 + 7 项回归测试

## 五、环境与约定

- Windows 11，Node 22，Electron 用最新稳定版
- 路径含空格/中文时注意引号转义
- 页面文案为中文；**代码与注释用英文**
- 所有交付物放 `D:\Jovan's Workplace`，完成后列出文件清单与启动方式

## 六、开工前复述（先答这五问，全部答对再写代码）

1. 技术栈选什么？为什么？
2. 数据文件（workspace.json）由谁读写？怎么保证不损坏？
3. ima API Key 存在哪？能不能进网络？
4. 主程序下载后做什么校验？
5. 关闭窗口时应用做什么？

---

*交接日期：2026-08-27 · 由方案讨论会（本会话）定稿*
