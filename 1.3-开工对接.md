# Jovan's Workplace · 1.3 开工对接文档（执行版草案，待确认）

> 日期：2026-09-06
> 状态：**待用户确认；确认前不得修改 `app/`、`data/` 或运行时行为。**
> 依据：项目交接、功能蓝图、1.3 更新交接、1.2 交接/危险报告及当前代码审查。

---

## 0. 本期目标与边界

本次目标升级为：先收口可靠性缺口，完成 1.3 八项需求，并将 `AI功能蓝图.md` 中尚未实现的多模态、文件翻译、搜索、MCP、运行时提醒、云端语音与 Galgame 分叉会话纳入同一夜间执行计划。

**本期做：**批 0 收口 + 主页回退、周期日程、学习重编排、灵魂碎片、消息操作、1.2 回归、UI 优化、双语。
**本期不做：**真流式 Agent、后台自主 Agent、`delegate_to_subject` 学科委派、发行打包、旧数据迁移脚本。
**不动：**用户 `data/` 文件内容；recurrence 仅添加可选规则字段，绝不展开为 52 条持久化记录。

## 1. 红线与纪律

- 零新增 npm 运行时依赖；Electron 44、纯 JS、`node:sqlite` 不变。
- renderer 禁 `window.prompt` / `window.confirm`；输入走内联或现有 modal。
- 只写 `D:\Jovan's Workplace\`；不读写工作区外内容。
- `data/` 是用户资产：不删、不清空、不手工改写；测试用隔离临时目录，结束清理并记录。
- AI 写业务数据必须仍为：draft → 用户确认 → 主进程校验写入。
- 文档与代码冲突、数据语义不清、范围扩大时，立即停下询问，不自行定方案。
- 每批结束：语法/针对性回归、`git status` 核验 → 单独提交并 push main → 一段简交。

## 2. 分批与模型策略

| 批次 | 内容 | 建议模型 | 完成门槛 |
|---|---|---|---|
| **批 0** | dsh 收口、regen 历史真实化、resolver 会话隔离 | Sol（最高） | 三项均有回归验证 |
| **批 1** | 主页回退、recurrence、消息操作、1.2 回归 | Sol（最高） | 数据模型和所有视图一致 |
| **批 2** | 学习书架、i18n、UI 自主优化 | Terra 主体；Luna 细节 | 主路径与双语可用 |
| **批 3** | 灵魂碎片最小版 | Sol（最高） | 并发会话不串线 |
| **批 4** | 图片/截图讲题、PDF/Word/PPTX 翻译与生成 | Sol 核心；Terra UI | 可真实处理三类文件 |
| **批 5** | 无 Key 搜索、可配置 MCP、运行时提醒、云端 ASR | Sol 核心；Luna UI | 无 Key 主路径可用，需 Key 功能可配置 |
| **批 6** | 完整 Galgame 分叉、回溯总结、树可视化 | Sol（最高） | 分支可创建、切换、回溯、总结 |
| 文档 | 简交、最终交接、危险报告 | Terra（中） | 可追溯验收与操作 |

---

## 3. 批 0：可靠性收口（后续功能的前置门槛）

### 0.1 dsh 收口

**现状冲突：**主 Agent 已内置网关，renderer 仍有 `127.0.0.1:8787` 的 birthday/conv/translate/ask/probe 分支。

**实施：**

1. 生日与农历转换只走 `window.workplace` 主进程实现，桌面版不得依赖 8787。
2. 旧网页预览兼容只能留在非 Electron 路径，桌面版不得探测或提示 dsh。
3. 课件翻译/问答优先迁入既有内置网关；若文件工作流缺少必要设计，停止该子项并报告，不能临时引桥、外部服务或依赖。
4. 清理桌面端 bridge 请求/提示，并同步 README 等旧说明。

**验收：**桌面 renderer 不含实际 `127.0.0.1:8787`/`BRIDGE` 请求；生日/农历回归通过；课件功能要么内置可用、要么诚实禁用且有中文说明，不能再显示“桥未连接”。

### 0.2 regen 历史真实化

**问题：**重生成只从 UI/内存移走旧 assistant，SQLite 仍追加新 assistant，历史和下次上下文不真实。

**实施：**

- `messages` 新增可选版本字段（`status='active'|'superseded'` 或等价设计）；旧记录默认 active，零迁移脚本。
- 重生成由主进程按明确 message id 将旧 assistant 标记 superseded，再保存新版本；默认加载、上下文和历史仅取 active。
- 每条 AI 消息存可选 `model/provider/usage/cost/tool_trace` metadata；旧消息显示“—”。
- 不物理删除旧回答，保留可追溯性。

**停下条件：**若 reload/UI 无法可靠保留刚刚重生成的 assistant message id，停止并给替代设计，禁止按文本模糊匹配。

**验收：**重生成后默认历史与模型上下文只取新回答；旧回答数据库可追溯；费用/模型 metadata 正确显示或优雅降级。

### 0.3 ask/plan resolver 按会话隔离

**问题：**`_askResolve` / `_planResolve` 是全局单例，未来主线和子对话会互相覆盖。

**实施：**

- wait/answer ask 与 plan 改为 `Map<sessionId, resolver>`。
- `ai:agent`、事件、answer IPC、plan IPC 均带 session id。
- 主线使用稳定 id；批 3 子对话使用独立 id；同会话仍串行、ask ≤3。
- 取消、窗口关闭、异常、超时清理该 session 的 pending resolver。

**验收：**两会话同时等待 ask 或 plan 时，各自回答回到原会话；取消一条不影响另一条；无残留 resolver。

---

## 4. 批 1：原需求 1、2、5、6

### 1. 主页回退

- 移除 dock fixed 覆盖与拖拽高度逻辑，恢复“上仪表、下 Agent”的普通滚动布局。
- 保留仪表折叠、卡片 hover 放大（默认保留）、思考条、ask/plan、收尾建议。
- Agent 区可在普通流内折叠为一行；默认展开，状态 localStorage 保存。

### 2. 周期日程

- `rows` 添加可选 `周期`：`{freq:'daily'|'weekly'|'monthly', weekday?, until?, count?}`；无字段即单次，旧数据零迁移。
- **只存一条规则，绝不持久化展开。**
- parse/detect 识别每天/每周一/每月 N 号；草稿默认一年，可改自定义日期或无限。
- recurrence 判断与实例展开集中实现，今日、日/周/月/列表、时间轴共享。
- 编辑/删除提供“仅此一次/所有重复”。“仅此一次”若需要例外记录字段，先停下确认数据模型，绝不临时发明字段。
- 重复任务只按最近一次进入逾期判断。

### 5. 消息操作与费用

AI 消息底部常驻复制、朗读、重生成、input/output tokens、RMB 费用；主 Agent、学科 AI、历史回放统一呈现，旧 metadata 显示“—”。

### 6. 1.2 GUI 回归

普通布局下回归思考条、ask 卡、计划卡、fallback、会话树重命名删除、收尾建议去重。

**批 1 验收：**“每周一上 CSC3160”只存一条规则、默认一年、所有视图每周一可见；旧单次数据不受影响；重复任务不重复逾期；主页无 dock 覆盖；每条 AI 消息操作/费用可见。

---

## 5. 批 2：原需求 3、7、8

### 3. 学习模块

采用 **A 书架式**：学期为顶部书架 Tab，科目为进度/最近使用卡；进入科目后是笔记、课件、学科 AI 三栏。仅改 renderer 信息架构，不改学习数据模型。

### 7. UI 自主优化

按 **B 信息架构 → C 关键路径动效 → A 细节打磨** 执行；沿用既有 CSS 变量和视觉语言，不造第二套风格；每一面回归后再进入下一面。

### 8. i18n

采用 **A 渐进提取**：`I18N={zh,en}`、`t(key)`、`data-i18n`。设置增加语言下拉，立即生效并 localStorage 保存；优先导航、按钮、设置、Agent、学习主路径；用户数据不翻译，未覆盖文案写入最终交接。

**批 2 验收：**学习书架主路径完整；中英切换刷新保持；关键操作清晰、动效不妨碍使用、风格统一。

---

## 6. 批 3：原需求 4，灵魂碎片最小版

- 主 Agent 运行时可创建临时子对话；以创建瞬间主线上下文快照为起点，不共享可变 `chatHistory`。
- 子对话有独立模型、状态、session id，可并发问答或使用允许的轻工具。
- 侧栏参考 Codex 边栏 Agent，可收起，标记“继承主线 · 待归档”。
- “归档到主线”由 LLM 提炼结论/要点，先显示确认预览，再追加主线；可选 L3 事实候选仍须用户确认。
- 子对话不得绕过 draft 写业务数据；ask/plan 必须使用批 0 隔离能力。

**批 3 验收：**主线运行时子对话独立可用；归档回主线可见、不污染原消息；主子 ask/plan 不串线；关闭/取消无等待残留。

---

## 7. 批 4：多模态与文件翻译

### 图片与截图讲题

- 支持粘贴、拖拽和桌面截图；图像以 data URL 交给已配置视觉模型。
- 视觉模型采用现有多厂商配置中的可用视觉模型；未配置时提示到设置页，不能伪造成功。
- 截图、原图与结果默认不上传到项目仓库；用户明确保存时才进入 `data/generated/`。

### PDF / Word / PPTX 翻译

- 三类文件均支持选择、抽取文本、逐段翻译、左右对照、选中文本后翻译/解释、生成翻译后的文件。
- 生成文件落入 `data/generated/{pdf,word,pptx}/`，提供打开目录、重命名、删除和导出入口。
- 先探查 Electron/Node 已有能力；若某一格式的“保持原排版生成”需要新增运行时依赖，则改为保留内容和基本结构的可打开副本，并在交接中写明质量边界，禁止违反零依赖红线。

**批 4 验收：**图片/截图可获得模型回答；PDF、DOCX、PPTX 都至少完成“导入→文本翻译→生成可打开的结果文件”闭环；错误、缺少视觉模型或文件无法解析时有明确中文提示。

## 8. 批 5：搜索、MCP、提醒与语音

### 无 Key 搜索

- 实现无需用户 Key 的联网搜索适配器，结果标注来源和时间；失败时不阻断本地/模型知识回答。
- 搜索调用必须经 Agent 工具层，结果截断并可追溯；不得让模型自行编造网页结果。

### 可配置 MCP 客户端

- 提供设置页的 MCP server 配置模型、启停、连通性测试与工具列表；不预装或擅自连接任何 server。
- 配置仅作用于用户主动添加的 server；读写类工具仍纳入 JW 的确认策略。

### 应用运行时提醒

- 仅在 JW 运行期间基于本地日程/任务触发 toast/系统通知；不注册 Windows 任务、不改系统设置。
- 每条规则增加去重标记，避免同一事项重复轰炸。

### 云端语音识别

- 添加录音、停止、上传、转写、填入输入框主路径；提供 Provider/Key 配置入口。
- 默认选 OpenAI transcription 兼容实现；若用户现有可用 Key 不支持语音转写，则功能保留为“已配置后可用”，不借用文本模型 Key 伪装成功。

**批 5 验收：**无 Key 搜索真实返回可点击来源；MCP 可保存/校验配置而不自动连接；应用运行时提醒可触发且去重；配置可用语音 Key 后能完成录音到文本闭环。

## 9. 批 6：Galgame 分叉会话

- 消息数据增加可选 `parent_message_id`、`branch_id`、`active`/版本 metadata；既有消息保持默认主线，零迁移。
- 任意历史节点可“从此续写”；新分支只带祖先链上下文，不能混入旁支消息。
- 左侧/弹层提供树可视化、分支切换、当前分支标记与 minimap 轻量导航；视觉采用剧情树，而非通用流程图。
- 回溯节点时：短对话直接显示；长分支调用模型生成摘要，摘要作为派生 metadata，不覆盖原文。
- 分支/重生成/灵魂碎片共用 message id 与版本模型，防止并行功能各造一套历史语义。

**批 6 验收：**可从任意历史节点创建和切换分支；每条分支上下文独立；回溯摘要可见且保留原文；重生成不会制造无标记重复消息；历史数据库可追溯分支关系。

## 10. 用户已拍板的执行决策

| 项 | 已确认决定 |
|---|---|
| 范围 | 全部蓝图，包括完整 Galgame |
| 数据加密 | 暂不做应用层加密；设置/说明须明确本地明文风险 |
| 文件 | PDF、Word、PPTX 全支持 |
| 搜索 | 无 Key 方案 |
| MCP | 可配置客户端，不预装 server |
| 提醒 | 仅应用运行时提醒 |
| 语音 | 云端 ASR；需独立可用 Key 时提供配置，不伪造可用 |
| Galgame | 完整分叉树、回溯总结与可视化 |
| 真实验收 | 允许使用现有 AI Key 做少量真实调用；不得输出或硬编码 Key |
| 交付标准 | 功能完整实现，允许少量 GUI 观感待用户复验 |
| 会话压缩 | 归档摘要，不物理删除原始消息；保留回溯与审计能力 |

## 11. 验证、提交、最终交付

每批最低验证：

```powershell
cd "D:\Jovan's Workplace\app"
node --check main.js
node --check ai.js
node --check tools.js
node --check conv.js
node --check data.js
node --check preload.js
node check-html.js
```

BrowserWindow 冒烟断言写文件。建议提交：

```text
fix(1.3-b0): reliability closure
feat(1.3-b1): recurrence and home flow
feat(1.3-b2): learning, i18n and UI polish
feat(1.3-b3): temporary sub conversations
feat(1.3-b4): multimodal and file translation
feat(1.3-b5): search, MCP, reminders and voice
feat(1.3-b6): branching conversations
```

最终交付：更新 `1.3-更新交接.md`（验收、代码清单、遗留、回归命令）和新增 `1.3-危险操作报告.md`（删除/覆盖、安装/下载、工作区外改动、git 提交、data 变更）。

## 12. 已知停下条件

用户已授权按本文件持续执行。仅以下情况暂停相关子项并询问：

1. recurrence “仅此一次”必须新增例外规则字段时；
2. 文件翻译任一格式无法在零运行时依赖下生成可打开产物时；
3. 云端 ASR 或用户选配 MCP 缺少其服务要求的独立凭据时；
4. 当前代码与本文件的数据语义发生冲突时。
