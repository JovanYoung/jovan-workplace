# Jovan's Workplace · 1.3 开工对接文档（正式执行版）

> 日期 2026-09-06 · 需求稿见 `1.3-更新交接.md`（八项需求与方案明细）· 本文定执行范围/顺序/验收
> 前置必读：`Codex-项目交接.md`（项目全记忆）、`AI功能蓝图.md`、`开工把关清单.md`
> **开工前完成第八节「八问复述」并经用户确认。**

---

## 一、范围总览（八项 + 分批）

**额度友好分批**（Codex Plus 5h 窗口约束）：

| 批 | 需求 | 难度 | 建议模型 |
|---|---|---|---|
| **批 1（硬核）** | ①主页回退 ②周期日程 recurrence ⑤消息操作条+费用 ⑥1.2 回归 | 高（数据模型）| GPT-5.6 Terra 主力 / Sol 局部 |
| **批 2（量大）** | ③学习模块编排(A) ⑦UI 自主优化 ⑧双语 i18n(A) | 中 | Terra + Luna |
| **批 3（新机制）** | ④子 Agent 灵魂碎片 | 中高 | Terra |

批间以提交+简短交接衔接；任一批完成即可先交付验收。

## 二、需求要点速查（细则见 1.3-更新交接.md）

1. **主页回退**：去 dock 悬浮 → 普通文档流（仪表区在上、Agent 区在下）；保留编排/思考条/折叠；hover 放大保留（拍板默认）
2. **周期日程**：rows 加可选 `周期` 字段（{freq: daily|weekly|monthly, weekday?, until?}）；**存规则不展开**；detect/parse 识别"每周一/每天/每月N号"；**默认 until=一年后** → 草稿卡展示并可改（自定义日期/无限）；日/周/月/列表/时间轴/今日区按规则展开；编辑提供"仅此一次/所有重复"；重复任务不重复逾期
3. **学习编排（A 书架默认）**：学期=书架 Tab → 科目卡片（进度/最近）→ 科目内 笔记/课件/学科AI 三栏；B/C 备选见需求稿
4. **灵魂碎片（最小版）**：主 Agent 运行时开并行子对话（快照主线上下文继承）→ 子对话可提问 → 「归档到主线」按钮：LLM 提炼要点回主线（追加上下文 + 可选 L3）；打开样式参照 Codex 边框 Agent（侧栏可收）。⚠️ ask/plan 单 resolver 与主线冲突需隔离（子对话禁用 ask/plan 或 resolver 按会话隔离）
5. **消息操作条**：AI 消息底部**常驻** [复制][朗读][重生成] + `本轮 tokens 输入X·输出Y ≈¥Z`（attachAiActions 扩展 usage/cost 参数；学科消息同步）
6. **1.2 GUI 回归**：dock 相关改验普通布局；思考条/问题卡/计划卡/fallback 提示/会话树/收尾建议实测
7. **UI 自主优化**：先跑应用审现状 → 发现清单 → 按 **B 信息架构 → C 动效 → A 细节** 顺序逐面优化（每面可回归）；保持现有风格语言
8. **双语 i18n（A 渐进）**：文案 key 化 → I18N zh/en → t() + data-i18n 刷新 → 设置加「语言」下拉（localStorage 记忆）；用户数据不翻译；范围=导航/按钮/设置/对话区主要界面，遗漏清单记录在案

## 三、明确不做（本期）

- MCP / 网页搜索 / 定时任务 / 语音输入 / 真流式 / delegate_to_subject 学科委派（蓝图 1.3 大设计，灵魂碎片先行）
- 旧数据迁移脚本（recurrence 为纯新增可选字段，零迁移）
- 不打包发行

## 四、技术约束与红线

- 零新 npm 运行时依赖；renderer 禁 window.prompt/confirm；只写 `D:\Jovan's Workplace\`
- recurrence：字段可选、写入走 data.add 既有通道；视图展开函数集中在单一工具文件（如 schedule.js 或 renderer 统一 util），各视图共用
- 前缀缓存红线：fixedSystem 保持纯常量，i18n 后 system prompt 若含文案须走常量模板
- ask/plan 单 resolver：灵魂碎片若与主线并发，需 resolver 按会话隔离或子对话禁 ask/plan（执行时二选一并记录）
- 数据文件非指令不动；删除先 git status；测试数据用完清理

## 五、验收（批 1 全过 + 批 2 全过 + 批 3 全过 = 1.3 完成）

批 1：主页普通布局可收折 ✅｜"每周一上 CSC3160"→ 草稿卡显示「每周一·至 2027-09-06」可改 → 确认后日历每周一可见、只存一条规则 ✅｜编辑提供仅此一次/所有重复 ✅｜消息下常驻操作条+费用 ✅｜重复任务今日区/逾期正确 ✅
批 2：学习 A 书架可完整操作（建学期/科目/笔记+学科AI入口）✅｜UI 优化每面可回归、风格统一 ✅｜语言切换即时生效+记忆、主要界面双语无漏 ✅
批 3：主 Agent 思考时可开子对话（上下文继承）✅｜归档按钮提炼回主线并可见 ✅｜主线 ask/plan 不受子对话干扰 ✅
全批：语法 0 failures｜既有功能回归（日程/备忘/主页对话/学科/记忆/技能）✅｜交接文档+危险操作报告 ✅

## 六、执行顺序

1. 读 1.3-更新交接.md 全稿 + Codex-项目交接.md + 现状代码（renderer 主页区/detect/parse/tools/data 结构）
2. 批 1：②recurrence（模型字段→解析→草稿卡→视图展开→编辑语义）→ ①主页回退 → ⑤操作条/费用 → ⑥回归
3. 批 2：⑧i18n 骨架先行（t() 与语言切换，后续文案渐进）→ ③学习编排 → ⑦UI 优化
4. 批 3：④灵魂碎片
5. 收尾回归 + 交接

## 七、环境坑（沿用 Codex-项目交接.md 第六节）

Electron 沙盒 flags / stdout 静默写文件 / safeStorage app name / JW_TEST_KEY 禁硬编码 / git 代理

## 八、开工前复述（八问）

1. recurrence 存什么、不存什么？默认周期范围是什么、怎么让用户改？
2. 主页回退保留什么、去掉什么？
3. 灵魂碎片怎么继承主线、怎么归档？与 ask/plan 单 resolver 怎么共存？
4. i18n 哪些翻译、哪些不翻译？切换怎么即时生效？
5. UI 优化三面顺序是什么？风格红线？
6. 操作条"常驻"与 hover 版差异？费用数据从哪来？
7. 红线六条背一遍。
8. 交付时交哪两份东西？

*交接日期 2026-09-06 · 执行建议 GPT-5.6 Terra（Sol 局部难点）*
