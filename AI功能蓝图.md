# Jovan's Workplace · Agent 功能蓝图（1.0.0+）

> 内部开发文档 · v2 定稿于 2026-09-01 · 所有决策经讨论确认
> 核心方向：**弃 dsh 桥 → 主进程内置 AI 网关（A 路自写最小 harness + 现成组件）→ 自研 Agent UI → 与工作台数据打通**

---

## 〇、路线判定：A 路（已定稿，勿再摇摆）

- **走 A 路**：自写最小 harness + 按需引入**现成组件**（Mem0 记忆库等）
- **B 路的定义**：引入 agent 编排框架（OpenAI Agents SDK / LangChain / Mastra）
- **为什么还是 A**：用户当前需求（多模型配置、token 计价、翻译 UI、Mem0 记忆）全部是**组件级**功能，A 路 + 现成组件完全覆盖；B 路只有在"多 Agent 协作 / 自主长任务 / 挂第三方工具链"出现时才考虑（触发信号不变）
- **引入 Mem0 ≠ 走 B 路**：Mem0 是记忆库（npm 组件），不是 agent 框架

---

## 一、版本节奏总览

| 版本 | 内容 | 状态 |
|---|---|---|
| **0.0.0** | 已发行（2026-08-28，GitHub Release）| ✅ |
| **1.0.0** | A（AI 网关·多模型）+ B（对话 UI）+ F1（全模块主动检测）+ 一句话解析迁移 | 未开始 |
| **1.1.0** | C（图片解析/截图讲题）+ D（课件翻译新 UI/生成文件）| 规划中 |
| **1.2.0** | E（学科 AI）+ F2（历史搜索）+ G（记忆·Mem0 直接上）| 规划中 |
| **1.3.0** | H（Galgame 分叉对话）| 概念期 |

---

## 二、1.0.0：内核 + 对话 UI（主体）

### A. 内置 AI 网关（弃 dsh）

- [ ] **预置市面常见 API 配置模板**：设置界面只需选厂商 + 填 API Key，**无需手动配 baseURL/模型名**
- [ ] 预置模板（全部 OpenAI 兼容接口）：
  | 厂商 | 默认 baseURL | 备注 |
  |---|---|---|
  | DeepSeek | `https://api.deepseek.com/v1` | 文本主力（V4 Flash/Pro）|
  | 智谱 GLM | `https://open.bigmodel.cn/api/paas/v4` | **视觉用 GLM-4.5V-Flash（免费）** |
  | 月之暗面 Kimi | `https://api.moonshot.cn/v1` | 视觉备选 |
  | OpenAI | `https://api.openai.com/v1` | 备选 |
  | 通义千问 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | 备选 |
  | 火山方舟 | `https://ark.cn-beijing.volces.com/api/v3` | 备选 |
  | 硅基流动 | `https://api.siliconflow.cn/v1` | 有免费模型 |
  | Ollama（本地）| `http://localhost:11434/v1` | 本地推理 |
- [ ] **多 Key 管理**：可同时配置多个厂商的 API Key，互不冲突
- [ ] **对话框自选 model**：下拉选择已配置的模型（显示厂商+模型名）
- [ ] **多模型自选 + 自定义模型名（已完成 M1 补丁）**：同厂商 Key 下列出全部预置模型（如 DeepSeek flash/pro）；设置面板显示每厂商模型列表，支持「自定义模型名」输入（逗号分隔，任意模型名，价格按厂商基准价估算）
- [ ] 视觉模型默认指向智谱 GLM-4.5V-Flash（免费）
- [ ] API Key 一律 safeStorage/DPAPI 加密存本地

### B. 自研 Agent 对话 UI
- [ ] 原生对话流：流式打字机输出 + 可中断
- [ ] 思维链 / 工具调用状态展示（步骤条）
- [ ] 模型切换：下拉自选已配置模型
- [ ] **思考深度调节滑块**（新增）：调节 V4 思考模式深度（关闭 / 浅 / 标准 / 深）
  - 关闭思考 → 最快最省（V4 非思考模式，FIM 补全仅此模式生效）
  - 深思考 → 质量最高但更贵（reasoning token 计入 output）
  - 实现：滑块映射模型推理参数（OpenAI 兼容 reasoning_effort 类字段；具体参数格式 M2 实测确认，实测文档第五节已记录"M1 暂不关闭思考模式"）
  - 联动：与 token/RMB 费用显示配合，调深时用户能看到费用变化
- [ ] 多会话管理：列表、清空、导入导出对话

### F1. Agent 主动检测 + 确认（扩展为全模块）
> 机制 = 意图检测 → 生成草稿 → 弹确认框 → 确认才写入（human-in-the-loop）

- [ ] **工作台内所有人工功能都要 Agent 检测并询问**，不止日程：

| 意图 | 例子 | 写入目标 |
|---|---|---|
| 日程 | "下周约导师" | 日程 |
| 任务 | "周五交计网作业" | 今日四象限 |
| 备忘 | "别忘了我妈不吃香菜" | 老忘记的事儿 |
| 亲友画像 | "我室友养了猫叫团子" | 亲友画像 |
| 学习 | "这周开始刷雅思听力" | 学习/备考 |
| 冲突检测 | 新日程撞已有日程 | 主动提示 |
| 逾期顺延 | 昨天没做完的事 | 提议顺延今天 |
| 生日临近 | 亲友生日快到 | 主动提醒 |

### 工具循环（生产级 5 要点）
| 要点 | 默认值 | 说明 |
|---|---|---|
| 循环上限 | 5 轮 | 防无限循环烧钱（简单任务 2-3 轮完）|
| 工具结果截断 | 2000 字符 | 防上下文爆炸 |
| 并行工具调用 | 开启 | 一次多个 tool_calls 合并执行 |
| 失败容错 | 重试 1 次 | 错误回填给模型自纠 |
| 写操作确认 | 开启 | 写数据类工具走确认，读操作直接执行 |

- [ ] **Token 消耗显示**：每次对话展示 token 用量 + 按当前 model 单价自动换算 **RMB 费用**（价格表内置在模型模板中，随模型模板可更新；单价以各厂商官方定价为准，标注"价格随官方调整"）

### 迁移（网页版已有，桌面端内置化）
- [ ] 一句话智能添加（自然语言解析：时间/地点/优先级 + 学习词典 + AI 深度解析）
- [ ] 课件翻译 / 课件提问（UI 见 1.1.0 D）

---

## 三、1.1.0：多模态 + 文件处理

### C. 多模态
- [ ] **图片解析**：粘贴/拖拽图片 → base64 → 视觉模型（默认智谱 GLM-4.5V-Flash 免费）
- [ ] **截图讲题**：Electron desktopCapturer 截屏 / 剪贴板图片 → 视觉模型讲解

### D. 课件翻译（新 UI 定稿）
- [ ] **左右分栏**：左 = 原文件，右 = 翻译后文件；**可一键切换左右位置**
- [ ] **选中文字交互**：在原文中选中词句 → 浮出「翻译 / 解释」按钮 → 点击后侧栏出现 Agent 处理并回复
- [ ] **侧栏 Agent**：翻译/解释结果输出在侧栏对话流中，可连续追问
- [ ] Agent 生成文件：pdf / word / py 脚本 → 存 `data\generated\{pdf,word,script}\`，页面列表管理（打开目录/重命名/删除/导出另存）

### 数据存储方案（v2 定稿：全本地优先 + 加密）
| 数据 | 位置 | 策略 |
|---|---|---|
| 全部数据（业务 + 聊天 + 生成文件）| **用户自选位置**的文件夹（`{数据目录}\` 下分 `workspace.json` / `conversations\` / `generated\`）| **本地优先 + 加密** |
| 加密 | 数据目录整体加密（可选；依赖系统 BitLocker 或应用层加密）| 高隐私档 |
| 云备份 | **只推荐 ima**，不强制；**勾选后才出现 ima API Key 输入框** | 业务数据随备份；聊天/文件不进云 |

---

## 四、1.2.0：学科 AI + 搜索 + 记忆

### E. 学科 AI（学习模块内独立对话）
- [ ] 每学科独立对话窗口（数学/英语/计网…），各自独立上下文
- [ ] 实现：一个 LLM 客户端 + 会话管理器（createConversation('数学')）→ 历史文件 `data\conversations\数学.json`
- [ ] UI：学习模块内嵌"学科对话"（左侧学科列表，右侧对话流）
- [ ] **会话内窗口压缩**：超 20 轮 → 旧消息 LLM 摘要化（自实现 ~30 行）

### F2. 搜索聊天历史
- [ ] 本地 SQLite FTS5："上次说的会议"秒级关键词命中
- [ ] 进阶：语义搜索 → sqlite-vec 向量列

### 学科检索（三层）
```
提问 → ① 本地检索（对话历史 + 上传的笔记/课件，FTS+向量）
     → ② 本地未命中 → 联网搜索（Tavily API 免费额度 / 自托管 SearXNG）
     → ③ 模型自身知识兜底
```

### G. 记忆系统（v2 定稿：直接上 Mem0，不再等积累）
- [ ] **直接用 Mem0**（npm 直装、本地向量存储、自动提炼事实）
- [ ] 适用：通用 Agent 跨会话记忆（记住用户偏好/背景）
- [ ] 学科 AI 的记忆 = 会话内摘要 + 资料检索（不需要 Mem0，两套机制分开）
- [ ] ❌ 不用豆包/火山云记忆（违背本地优先）、Zep（自托管重）、LangMem（绑 LangChain）、Letta（研究向重）

---

## 五、1.3.0：Galgame 分叉对话（H 模块，概念期）

### 需求
- 同一对话框内，对话历史像 galgame 剧情树**分叉 + 可视化**，可回溯到任意节点重新续写
- **回溯时 AI 自动总结该段对话大意**（短则直接展示全部）——原创点，现有开源无此功能
- DeepSeek 网页端有"编辑重发"但无可视化

### 查证结果（2026-09-01，GitHub 已有人开源，组合无现成）
| 项目 | 亮点 | 差距 |
|---|---|---|
| gitchat（Jaeha0526）| 消息=commit、分叉=git branch、树可视化、AI 人设 | 流程图风格非 galgame；无回溯总结 |
| GitChat（DrustZ）| 分支/合并/重连、级联重生成、minimap、React Flow | 偏流程图编辑器；无总结 |
| ai-root-chat | 纯分支聊天隔离上下文 | 无树可视化 |
| llm-node-chat | 分支+树+多模型 | 半成品 |
| ChatGIT（扩展）| 给 ChatGPT 网页加分支导航 | 仅浏览器扩展 |

### 差异化定位（站在巨人肩膀上）
- 数据结构借鉴 gitchat（消息=节点、分支=树）
- **UI 自研 galgame 剧情树视觉**（现有全是技术流程图，视觉是空白）
- **回溯自动总结**（原创点）
- 集成进工作台（与学科 AI/日程打通）
- 若做得好 → 可作为独立开源项目（先证明，再开源）

---

## 六、发行遗留待办（0.0.0 → 1.0.0 前处理）

- [ ] **下载器端到端实测**（用户桌面双击跑完整三步引导流程）
- [ ] GUI 实测：回收站 / 导出导入对话框 / 学习模块
- [ ] 旧数据接管演练（D:\dsh-data 一键接管真实数据）
- [ ] **SHA256 联动**：改主程序 → zip 哈希变 → 下载器 EXPECTED_SHA256 同步更新 + 重打包
- [ ] 版本升级链路：版本号 + 新 zip + 新 SHA256 + 重打包下载器 + 新 Release
- [ ] 网页版与桌面版分叉管理（两份代码，改动需同步）

---

## 七、架构决策备忘（v2）

- **A 路（自写最小 harness + 现成组件）为主**，不引 agent 编排框架
- B 路（框架）触发信号：①"帮我做…"多步自主任务 ②跨会话记忆（已由 Mem0 承担，降级）③挂第三方工具链（搜索/代码执行）；届时只在 Agent 模块内引入
- 多会话 = harness 基础能力（消息数组 + 持久化），不是框架馈赠
- 上下文压缩 vs 检索：通用 Agent 用窗口摘要（压缩）；学科 AI 用 FTS+向量（检索注入）
- **记忆：Mem0 直接上（v2 变更，不再"先积累后升级"）**；学科 AI 记忆 = 摘要 + 检索
- 工具循环参数：工程常量非 ML，默认值抄开源（OpenAI Agents SDK max_turns=10 等），config.json 可调
- **token 计价**：价格表内置模型模板，按官方定价可更新；显示 RMB 费用
- **API 配置**：厂商模板预置，用户只填 Key；多 Key 并存；对话框自选 model
- **数据隐私分级（v2）**：全部数据本地优先 + 加密（自选位置文件夹）；云备份仅推荐 ima（勾选才填 Key），聊天/文件不进云
- 视觉模型默认智谱 GLM-4.5V-Flash（免费）
