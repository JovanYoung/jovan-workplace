# Jovan's Workplace · Agent 功能蓝图（1.0.0+）

> 内部开发文档 · 定稿于 2026-09-01 · 所有决策经讨论确认
> 核心方向：**弃 dsh 桥 → 主进程内置 AI 网关（A 路自写最小 harness）→ 自研 Agent UI → 与工作台数据打通**

---

## 一、版本节奏总览

| 版本 | 内容 | 状态 |
|---|---|---|
| **0.0.0** | 已发行（2026-08-28，GitHub Release）| ✅ |
| **1.0.0** | A（AI 网关）+ B（对话 UI）+ F1（日程确认）+ 一句话解析迁移 | 未开始 |
| **1.1.0** | C（图片解析/截图讲题）+ D（PPT 翻译/生成文件）| 规划中 |
| **1.2.0** | E（学科 AI）+ F2（历史搜索）+ G（记忆自建）| 规划中 |
| **1.3.0** | H（Galgame 分叉对话）| 概念期 |

---

## 二、1.0.0：内核 + 对话 UI（主体）

### A. 内置 AI 网关（弃 dsh）
- [ ] 不再依赖 dsh / 8787 / 3080 端口；主进程直接调 LLM API，别人装完即用
- [ ] **双模型路由**：文本 = DeepSeek V4（对话/解析/翻译）；视觉 = Kimi-VL / GLM-4.5V（图片/截图）
- [ ] API Key 设置（设置页输入，safeStorage/DPAPI 加密存本地，同 ima key 套路）
- [ ] OpenAI 兼容接口：仅需 baseURL + Key，多模型可配

### B. 自研 Agent 对话 UI
- [ ] 原生对话流：流式打字机输出 + 可中断
- [ ] 思维链 / 工具调用状态展示（步骤条）
- [ ] 模型切换：V4 Flash（快省）/ V4 Pro（深度）
- [ ] 多会话管理：列表、清空、导入导出对话

### F1. Agent 主动提议加日程（human-in-the-loop）
```
检测到日程意图 → 调 addScheduleDraft（不写库）→ 前端弹确认框
→ 用户确认才真正写入数据层（contextBridge → workspace.json）
```

### 工具循环（生产级 5 要点）
| 要点 | 默认值 | 说明 |
|---|---|---|
| 循环上限 | 5 轮 | 防无限循环烧钱（简单任务 2-3 轮完）|
| 工具结果截断 | 2000 字符 | 防上下文爆炸 |
| 并行工具调用 | 开启 | 一次多个 tool_calls 合并执行 |
| 失败容错 | 重试 1 次 | 错误回填给模型自纠 |
| 写操作确认 | 开启 | 写数据类工具走确认，读操作直接执行 |

> 参数全部放 config.json 可调。**无需机器学习**：这些是工程常量（启发式），参考 OpenAI Agents SDK（max_turns=10）等开源默认值。

### 迁移（网页版已有，桌面端内置化）
- [ ] 一句话智能添加（自然语言解析：时间/地点/优先级 + 学习词典 + AI 深度解析）
- [ ] 课件翻译 / 课件提问

---

## 三、1.1.0：多模态 + 文件处理

### C. 多模态
- [ ] **图片解析**：粘贴/拖拽图片 → base64 → 视觉模型
- [ ] **截图讲题**：Electron desktopCapturer 截屏 / 剪贴板图片 → 视觉模型讲解

### D. 文件处理
- [ ] **幻灯片自动翻译**：解析 pptx（zip/XML）→ 逐页翻译 → 写回/导出
- [ ] **Agent 生成文件**：pdf / word / py 脚本 → 存 `data\generated\{pdf,word,script}\`，页面列表管理（打开目录/重命名/删除/导出另存 showSaveDialog）

### 数据存储方案（定稿）
| 数据 | 位置 | 策略 |
|---|---|---|
| 聊天记录 | 本地 `data\conversations\`（SQLite，FTS5 全文索引）| **不上 ima**（隐私+体积）；随全局备份轮换；可选加密 |
| 生成文件 | 本地 `data\generated\` | 同上；不上云，主动分享才导出 |

---

## 四、1.2.0：学科 AI + 搜索 + 记忆

### E. 学科 AI（学习模块内独立对话）
- [ ] 每学科独立对话窗口（数学/英语/计网…），各自独立上下文
- [ ] 实现：一个 LLM 客户端 + 会话管理器（createConversation('数学')）→ 历史文件 `data\conversations\数学.json`
- [ ] UI：学习模块内嵌"学科对话"（左侧学科列表，右侧对话流）
- [ ] **会话内窗口压缩**：超 20 轮 → 旧消息 LLM 摘要化（自实现 ~30 行，不用框架）

### F2. 搜索聊天历史
- [ ] 本地 SQLite FTS5："上次说的会议"秒级关键词命中
- [ ] 进阶：语义搜索 → sqlite-vec 向量列

### 学科检索（三层，问题 1b 定稿）
```
提问 → ① 本地检索（对话历史 + 上传的笔记/课件，FTS+向量）
     → ② 本地未命中 → 联网搜索（Tavily API 免费额度 / 自托管 SearXNG）
     → ③ 模型自身知识兜底
```

### G. 记忆系统（渐进式）
- [ ] 先自建：学科摘要注入 + FTS 检索（1.2 够用）
- [ ] 升级路径：数据积累几百条对话后 → **Mem0**（仅通用 Agent；npm 直装、本地向量、自动提炼事实）
- [ ] ❌ 不用豆包/火山引擎云记忆（违背本地优先）；不用 Zep（要自托管服务）、LangMem（绑 LangChain）、Letta（太重）

### 主动检测场景（Agent 主动提议，同一套机制挂不同工具）
| 意图 | 例子 | 写入目标 |
|---|---|---|
| 日程 | "下周约导师" | 日程 |
| 任务 | "周五交计网作业" | 今日四象限 |
| 备忘 | "别忘了我妈不吃香菜" | 老忘记的事儿 |
| 亲友画像 | "我室友养了猫叫团子" | 亲友画像 |
| 学习 | "这周开始刷雅思听力" | 学习/备考 |
| 冲突检测 | 新日程撞已有日程 | 主动提示 |
| 逾期顺延 | 昨天没做完的事 | 提议顺延今天 |
| 生日临近 | 亲友生日快到 | 主动提醒 |

---

## 五、1.3.0：Galgame 分叉对话（H 模块，概念期）

### 需求
- 同一对话框内，对话历史像 galgame 剧情树一样**分叉 + 可视化**，可回溯到任意节点重新续写
- **回溯时 AI 自动总结该段对话大意**（短则直接展示全部）——原创点，现有开源无此功能
- DeepSeek 网页端有"编辑重发"但无可视化

### 查证结果（2026-09-01，GitHub 已有人开源，但组合无现成）
| 项目 | 亮点 | 差距 |
|---|---|---|
| gitchat（Jaeha0526）| 消息=commit、分叉=git branch、树可视化、AI 人设 | 流程图风格非 galgame；无回溯总结 |
| GitChat（DrustZ）| 分支/合并/重连、级联重生成、minimap、React Flow | 偏流程图编辑器；无总结 |
| ai-root-chat | 纯分支聊天隔离上下文 | 无树可视化 |
| llm-node-chat | 分支+树+多模型 | 半成品 |
| ChatGIT（扩展）| 给 ChatGPT 网页加分支导航 | 仅浏览器扩展 |

### 差异化定位（站在巨人肩膀上）
- 数据结构借鉴 gitchat（消息=节点、分支=树）
- **UI 自研 galgame 剧情树视觉**（现有全是技术流程图，视觉是空白）
- **回溯自动总结**（原创点）
- 集成进工作台（与学科 AI/日程打通，现有全是独立 App）
- 若做得好 → 可作为独立开源项目（史诗级想法，但需先证明）

---

## 六、发行遗留待办（0.0.0 → 1.0.0 前处理）

- [ ] **下载器端到端实测**（用户桌面双击跑完整三步引导流程）
- [ ] GUI 实测：回收站 / 导出导入对话框 / 学习模块
- [ ] 旧数据接管演练（D:\dsh-data 一键接管真实数据）
- [ ] **SHA256 联动**：改主程序 → zip 哈希变 → 下载器 EXPECTED_SHA256 同步更新 + 重打包
- [ ] 版本升级链路：版本号 + 新 zip + 新 SHA256 + 重打包下载器 + 新 Release
- [ ] 网页版与桌面版分叉管理（两份代码，改动需同步）

---

## 七、架构决策备忘

- **A 路（自写最小 harness）为主**，不引重型框架（OpenAI Agents SDK / LangChain / Mastra）
- B 路（框架）触发信号：①"帮我做…"多步自主任务 ②跨会话记忆 ③挂第三方工具（搜索/代码执行）；届时只在 Agent 模块内引入，不伤其他模块
- 多会话 = harness 基础能力（消息数组 + 持久化），不是框架馈赠
- 上下文压缩 vs 检索：通用 Agent 用窗口摘要（压缩）；学科 AI 用 FTS+向量（检索注入）
- 记忆框架选型：Mem0（唯一真正需要的，先自建后升级）
- 工具循环参数：工程常量非 ML，默认值抄开源，config.json 可调
- 数据隐私分级：聊天记录/生成文件 = 高隐私（本地+可选加密，不上云）；业务数据 = 随全局备份；ima 只备份业务数据
