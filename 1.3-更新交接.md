# Jovan's Workplace · 1.3 更新交接（下一期需求）

> 日期 2026-09-06 · 主 PM 汇总用户反馈 → 执行会话开工前需用户拍板「待定项」。
> 前置必读：`Codex-项目交接.md`（项目全记忆）、`AI功能蓝图.md`。

---

## 一、需求总览（六块）

| # | 需求 | 类型 | 状态 |
|---|---|---|---|
| 1 | 主页 UI 回退（1.2 dock 未达预期）| UI 重构 | 待执行 |
| 2 | 周期性日程（"每周一上 CSC3160"→ 默认排一年 + 询问自定义）| 新功能（数据模型）| 待执行 |
| 3 | 学习模块重新编排（功能齐但乱）| UI/UX 重构 | ⚠️ 方案待用户拍板 |
| 4 | 子 Agent 对话（灵魂碎片：继承主线记忆 → 聊完归档回流）| 新功能（多 Agent UI）| 待执行 |
| 5 | AI 消息下直接显示：复制/朗读/重生成 + token/费用（现 hover+角落，没被看到）| UI 优化 | 待执行 |
| 6 | （延续 1.2 待 GUI 确认项一并回归）| — | 待执行 |

---

## 二、需求 1：主页 UI 回退

**用户反馈**：1.2 的 Agent 悬浮中下 dock（#agentDock）"完全没有做到预期的结果"，要求回退。

**回退目标**：
- 去掉悬浮 dock 定位（fixed/覆盖），**改回普通文档流布局**（1.1 结构）：主页 = 上方仪表盘区 + 下方 Agent 对话区（普通区块，滚动随主页）
- 保留：仪表卡 hover 放大（有反馈认为可以）、可折叠仪表、思考条、ask/plan/收尾（编排功能与布局解耦，全部保留）
- 收起交互：若有需要，Agent 区顶部保留"折叠为一行"切换（普通流内折叠，非悬浮）
- **待定**：hover 放大去留（默认保留）；Agent 区折叠默认态

## 三、需求 2：周期性日程（recurring）——核心设计

**场景**：用户说"每周一都要上 CSC3160" → 现在只排下周一。要求：**模型默认安排一年（52 周）**该日程，然后询问是否自定义周期范围。

### 数据模型（改动 workspace.json rows）
- 新增字段：`周期`（recurrence）可选：
  ```json
  { "标题": "CSC3160", "类型": "日程", "周期": { "freq": "weekly", "weekday": 1, "until": "2027-09-01" } }
  // freq: daily|weekly|monthly; weekly 必带 weekday; 可选 until/次数
  ```
- 兼容：无 周期 字段 = 单次（旧数据零迁移成本）
- 去重：一次"每周一"只存一条规则记录，**不展开成 52 条**（存规则，视图层按日期展开）

### 视图展开（各视图按规则计算该日期是否命中）
- 日视图/今日区：今天命中规则的日程照常显示
- 周/日历/列表视图：按规则展开命中项
- 时间轴：命中即显示；"编辑/删除"提供「仅此一次 / 所有重复」选择（日历 App 惯例）
- 逾期/今日待处理：重复任务不重复逾期（只计最近一次）

### 解析与交互（用户点名流程）
1. detect/parse 识别周期意图（"每周一/每天早上/每月1号"等词）
2. **默认生成规则：weekly 该 weekday，until = 今天起一年后** → 草稿卡明确展示：「📅 CSC3160 · 每周一 · 持续至 2027-09-06」
3. 草稿卡含**周期编辑**：「持续到？[一年(默认)] [自定义日期] [无限]」——确认/改完才写入
4. ask/plan 机制可复用于此询问（或草稿卡内联编辑，执行时定）

### 影响面
- data.js：无改动（行内新字段）
- parse.js detect/parse：识别"每周/每天/每月"→ 产 recurrence
- 今日区/日/周/日历/列表/时间轴渲染：按规则展开
- detect 的 queryToday：周期任务"今天该做"判断
- 编辑卡片：周期展示 + 单次/全部操作

## 四、需求 3：学习模块重新编排（方案待用户拍板）

**现状问题**：学期→科目→笔记/课件 + 备考 + 学科 AI 入口散乱、层级深、来回切累。

**候选方案（选一或组合）**：
- **A. 书架式**：顶部"学期"为书架 Tab → 科目为卡片（进度/最近使用）→ 点开科目 = 笔记/课件/学科 AI 三栏布局（左列表中内容右 AI）——参照 Notion/GoodNotes 科目本
- **B. 目录+预览**：左侧目录树（学期▸科目▸资料），右侧内容预览 + 可呼出学科 AI 侧栏——像 Obsidian/IDE
- **C. 卡片流+快捷**：全平铺卡片（近期科目/备考/学科AI快捷入口置顶），点击放大——轻量、少层级

**执行前需用户选 A/B/C**（或混合）。选定后按"合适编排"重构 renderer 学习区（数据模型不动）。

## 五、需求 4：子 Agent 对话（灵魂碎片）

**用户诉求**：主 Agent 思考可能持续很久（如 1 小时），期间无法并行其他对话 → 支持**临时子 Agent 对话**：
- 主 Agent 思考时仍可开子对话（另一线程/会话），子 Agent **继承主线对话记忆**（带上下文快照）
- 子对话可独立提问/处理事务
- 聊完有按钮「**归档到主线记忆**」：把子对话结论/要点提炼回主线（塞回主灵魂）——如同一片灵魂碎片用完塞回
- 打开方式参考 **Codex 边栏 Agent** 风格（侧边展开，可收起）

**实现建议**（结合 1.3 多 Agent 蓝图）：
- 复用一个并发"轻量会话"：主线 runAgentLoop 在跑时，renderer 仍可发第二个对话请求（主进程不阻塞——ai.js 调用是 async，Electron IPC 天然并行；需处理单 resolver 假设：ask/plan 的 waitAsk/waitPlan 是全局单 resolver，若主线程阻塞在 ask 会冲突——子对话**不走 ask/plan**（纯问答/轻工具）或 ask resolver 按会话隔离）
- 子对话继承主线：快照主线当前 chatHistory 作为子对话初始上下文
- 归档回流：子对话结尾按钮 → 把子对话摘要 + 要点经 LLM 提炼成一段 → 追加为主线对话上下文的一条 user/记忆（或 mem confirm 到 L3 + 追加主线消息）
- UI：主页 Agent 区顶部"＋子对话"按钮 → 侧栏展开（Codex 边框样式）→ 顶部标记"继承主线 · 待归档"

**范围控制**：本期做"临时并行子对话（继承+归档）"；学科委派 delegate_to_subject（蓝图 1.3）仍后续。

## 六、需求 5：AI 消息操作 UI（让用户看得到）

**现状**：操作条 hover 才出现（opacity 0）、token/费用显示在 #chatMeta（角落），用户完全没注意到。

**目标**：每条 AI 消息**底部常驻一行**：
```
[复制] [朗读] [重生成]         · 本轮 tokens: 输入 X · 输出 Y · ≈¥Z
```
- 操作按钮由 hover 显示改为**常显**（或至少图标常显）
- **费用直接贴在该条消息下**（attachAiActions 扩展：接受 usage/cost 参数渲染）
- 学科 AI 消息同样处理
- 历史消息回显时（重开会话）保留费用展示（若存了 usage 则显，未存显"—"）

## 七、1.2 待 GUI 确认项（并入 1.3 回归）

思考条 ask/plan 状态 / fallback 提示 / 模糊指令问题卡 / 计划卡 / dock 相关（回退后改验普通布局）/ 会话树重命名删除 / 收尾建议去重。

## 八、执行前置（开工前用户拍板）

1. 学习模块选 A/B/C（或混合）
2. hover 放大去留（默认保留）
3. 周期日程确认的交互形态（草稿卡内联 vs ask 卡）
4. 子对话是否本期做"继承+归档"最小版（建议是）

## 九、技术约束 & 环境坑 & 交接要求（沿用，见 Codex-项目交接.md 五/六节）

- 新增 recurrence 是**数据模型扩展**：字段可选、旧数据零迁移、写入走 data.add 既有通道
- renderer 禁 prompt/confirm；零新 npm 依赖（SQLite 已内置）
- 交付 = 交接文档 + 危险操作报告
