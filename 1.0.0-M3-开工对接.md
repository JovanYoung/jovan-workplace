# Jovan's Workplace · 1.0.0 M3 开工对接文档（学科 AI + 历史搜索）

> 唯一交接文档。**开工前先完成第九节「五问复述」并得到确认。**
> 需求总账见同目录 `AI功能蓝图.md`（v3）；M1/M2 已完成（网关 + 工具循环 + 主动检测，commit 7a1f7bc / 1eba1f0）。

---

## 一、当前状态（M2 已完成）

```
app\
├── main.js        窗口/托盘/数据目录/ima key/IPC（data:*, ai:* 系列）
├── ai.js          AI 网关：8 厂商模板/流式 chat/runAgentLoop(工具循环)/jsonOutput/THINKING_LEVELS(思考滑块)
├── tools.js       10 工具（5 读直接执行 + 5 写草稿待确认）
├── parse.js       一句话解析（ai:parse + todayCN 注入）+ detect 8 类意图 + 学习词典（dict.json）
├── data.js        数据层（workspace.json 原子读写/快照/备份）
├── preload.js     window.workplace.*（含 aiAgent/aiParse/aiDetect/aiConfirmDraft/aiDict*）
└── renderer\index.html  五大模块 + Agent 对话（思考滑块/步骤条/draft 确认卡）+ 设置 AI 面板
```

**M2 已知限制（本 M3 一并修复）**：
1. 相对日期跨月边界仍可能误差（如"下周三"恰在月末）→ `normalizeDate` 做本地兜底
2. 低置信度未接「AI 深度解析（pro）」入口 → UI 补切换
3. detect 冲突/逾期靠模型理解，未做本地数据比对 → detect 前注入 `query_today` 结果
4. 主动检测无防抖，连续多轮可能重复弹框 → 加"本轮已确认不重复"去重

---

## 二、M3 范围（四步）

### Step 1：会话管理器（学科 AI 的地基，主进程新模块 conv.js）
- [ ] `createConversation(subject)` / `listConversations()` / `loadConversation(id)` / `appendMessage(id, msg)`
- [ ] 会话历史持久化：`data\conversations\{学科名}.json`（或入 SQLite，见 Step 2）
- [ ] 每学科独立上下文：对话只带该学科自己的历史（消息数组）
- [ ] **窗口压缩（L1）**：单会话超 20 轮 → 最旧 10 轮调 LLM 压缩成摘要（`jsonOutput` 复用），摘要 + 最近 10 轮作为新上下文
- [ ] IPC：`conv:list` / `conv:create` / `conv:open` / `conv:send`（流式）/ `conv:rename` / `conv:clear`

### Step 2：历史搜索（F2 + 记忆 L2，主进程 SQLite）
- [ ] **SQLite 选型（零依赖）**：优先 `node:sqlite`（DatabaseSync——**已实测当前 Node 22.22.2 可用**；需在 Electron 44 主进程实测确认，若 Electron 内不可用则兜底 `sql.js` wasm 版）；**禁止引 better-sqlite3 等原生 npm 包**（需编译、ABI 风险）
- [ ] 库文件：`data\conversations.db`
  - `messages` 表：id / conv_id / role / content / ts
  - `conversations` 表：id / subject / title / created_at / updated_at
  - **FTS5 虚拟表**（content 索引）
- [ ] 会话 + 消息统一入 SQLite（Step 1 的 JSON 文件改为 SQLite 存储，保持一个库）
- [ ] 检索 IPC：`conv:search {q}` → FTS5 命中（消息 + 所属会话/学科/时间），renderer 展示命中列表，点击跳回对应会话
- [ ] 语义搜索（sqlite-vec）**本 M3 不做**（蓝图标注"进阶"，FTS 先行——Hermes 23 万星项目也是 FTS5 先行，方向已验证）

### Step 3：学科对话 UI + 档案注入（学习模块内嵌）
- [ ] 学习模块内新增「学科 AI」区：左侧学科列表（+ 新建/重命名/清空），右侧对话流（复用 Agent 对话框样式：流式/思考滑块/费用显示）
- [ ] 入口联动：从学习模块某学期/科目点「AI 问答」→ 自动开该科目会话
- [ ] **学科档案注入（L4 雏形）**：每次提问 → 用 FTS 检索该学科相关的**笔记/课件文本 + 备忘** top-5 → 注入 system prompt（"以下是你的{学科}资料摘要，回答优先基于它们"）
  - 数据源：学习模块笔记/课件（renderer 里 rows 中 type=课件/笔记 + 学科归属）→ 需要主进程能访问（data.js rows 已可读）
  - 注入失败/无资料 → 正常回答（模型知识兜底，三层检索的 ③）
- [ ] 模型/思考深度选择复用 Agent 面板（同厂商已配模型下拉）

### Step 4：M2 已知限制修复（顺手收尾）
- [ ] 跨月日期：parse 结果日期留空或明显错误时 → 本地 `normalizeDate` 兜底二次解析
- [ ] 低置信度入口：解析卡上低置信度时显示「🤖 AI 深度解析（pro）」（复用 aiDeepBtn 思路，pro 模型）
- [ ] detect 冲突/逾期：detect 前先查 `query_today`/近 7 天日程注入上下文，让模型基于真实数据判断
- [ ] 防抖去重：同一会话连续轮次 detect 命中同类意图且用户已忽略 → 不重复弹

---

## 三、明确不做（防范围蔓延）

- ❌ L3 事实记忆（Mem0/事实库）→ M4
- ❌ G2 自动技能提炼 → M4
- ❌ 技能库 / 技能管理 UI → M4
- ❌ Galgame 分叉对话 / 课件翻译新 UI / 图片解析 → M5+
- ❌ 语义向量搜索（sqlite-vec）→ 后续
- ❌ 不新增 npm 依赖（SQLite 用 node:sqlite 或 sql.js wasm）
- ❌ 不重新打包发行（1.0.0 收尾统一做）

---

## 四、技术约束

- 纯 JS + **Node 内置模块**；SQLite 优先 `node:sqlite`（DatabaseSync），Electron 内不可用时退 `sql.js`（wasm，纯文件、无原生编译）
- 对话/消息/会话统一 SQLite；学习词典 dict.json 保持不动
- AI 调用复用 ai.js 现有能力（chatOnce/jsonOutput/runAgentLoop 按需）
- 代码/注释英文；UI 文案中文；会话历史属高隐私 → 不随 ima 备份上传（沿用聊天记录"本地不上云"决策）
- 学科数据（笔记/课件）从 data.js rows 读取，不要新开存储

## 五、验收标准（全满足 + 交接文档 = M3 完成）

1. 学习模块 → 数学开对话 → 问「我笔记里记的微分中值定理条件是什么」→ 命中学科笔记 → 回答引用笔记内容 ✅
2. 学科对话能续：第二次提问记得前文（独立上下文，跟英语会话互不干扰）✅
3. 同一会话聊 25+ 轮 → 旧消息被压缩成摘要，上下文仍连贯、费用可看 ✅
4. 搜索「上次说的会议」→ 秒级命中历史消息，点击跳回对应会话 ✅
5. 说「下周三交作业」跨月边界 → 日期解析正确（本地兜底生效）✅
6. detect 不再重复弹同类确认框（连续对话实测）✅
7. 手动功能回归 + Agent 对话（M1/M2 功能）不受影响 ✅
8. 交接文档交付（实测结论：node:sqlite 在 Electron 44 可用性 / FTS5 建表 SQL / 学科注入效果 / 坑 / 改动清单）✅

## 六、执行顺序（每步能跑再继续）

1. 读 `AI功能蓝图.md`（v3）E/F2/G 段 + 现有 ai.js/tools.js/parse.js/data.js + renderer 学习模块区
2. **先实测 node:sqlite 在 Electron 44 可用性**（写 5 行脚本验证 DatabaseSync 建表/查询，可跑才用它，否则立刻切 sql.js）→ 定 SQLite 方案
3. Step 1 会话管理器（SQLite 版）+ 窗口压缩
4. Step 2 FTS5 历史搜索 + IPC
5. Step 3 学科对话 UI + 档案注入
6. Step 4 M2 限制修复
7. 本地 `env -u ELECTRON_RUN_AS_NODE NODE_OPTIONS= ./node_modules/.bin/electron . --no-sandbox --disable-gpu --disable-gpu-compositing` 全量回归 + 验收 8 项
8. **写交接文档**（必交）→ 提交推送（commit message 前缀 `feat(conv):` / `feat(m3):`）

## 七、环境坑（沙盒注入，照抄 M2 交接）

- Electron 脚本直接跑时：`app.setName("Jovan's Workplace")` + `setPath('userData', 正确路径)` 必须在 whenReady 前（safeStorage 解密乱码坑）
- 命令行加 `--no-sandbox --disable-gpu --disable-gpu-compositing`（GPU 沙盒崩溃）；真实桌面不需要
- 打包命令前 `NODE_OPTIONS=`；跑 Electron 前 `env -u ELECTRON_RUN_AS_NODE`
- git push 网络波动重试 / `-c http.proxy=` 绕过

## 八、交接文档要求（必交内容）

1. node:sqlite 在 Electron 44 的实测结论（可用/不可用 + 证据）
2. SQLite 建表 SQL（含 FTS5 索引）与库结构
3. 学科档案注入的效果样例（注入前后回答对比）
4. 窗口压缩的实测（多少轮触发、摘要质量）
5. 遇到的坑 + 代码改动清单

## 九、开工前复述（五问，全对再动手）

1. M3 的核心交付是什么（一句话）？
2. SQLite 用哪个实现？为什么不能引 better-sqlite3？
3. 学科 AI 和通用 Agent 的对话数据怎么隔离？
4. 学科档案注入的数据从哪来、怎么进 prompt？
5. 会话历史能上传 ima 云备份吗（为什么）？

---

*交接日期：2026-09-02 · 建议执行模型：DeepSeek V4 Pro（新会话）*
