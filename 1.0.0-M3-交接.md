# Jovan's Workplace · 1.0.0 M3 交接文档（学科 AI + 历史搜索）

> 唯一交接文档 · 交接日期 2026-09-02 · 对应开工文档 `1.0.0-M3-开工对接.md`
> M3 范围：Step 1 会话管理器 / Step 2 FTS5 历史搜索 / Step 3 学科对话 UI + 档案注入 / Step 4 M2 限制修复

---

## 一、交付状态总览

| Step | 内容 | 状态 |
|---|---|---|
| Step 0 | node:sqlite 在 Electron 44 实测 | ✅ 可用 |
| Step 1 | 会话管理器 conv.js + 窗口压缩 L1 | ✅ 完成 |
| Step 2 | SQLite + FTS5 历史搜索 + IPC | ✅ 完成 |
| Step 3 | 学科对话 UI + 档案注入 | ✅ 完成 |
| Step 4 | M2 四限制修复 | ✅ 完成 |
| 验收 | 代码级回归 + 启动回归 | ✅ 通过（真实 AI 交互见「遗留」） |
| 交接 | 本文档 | ✅ |

**改动文件清单**（`git status --short` 实测）：

| 文件 | 改动 |
|---|---|
| `app/conv.js` | **新增**：会话管理器 + 历史搜索（约 270 行） |
| `app/main.js` | require conv；whenReady 加 `conv.init()`；新增 7 个 `conv:*` IPC |
| `app/preload.js` | 暴露 `convList/convCreate/convOpen/convRename/convClear/convSearch/convSend`；`aiParse` 加 `pro` 参数 |
| `app/parse.js` | `pickDefaultModel(pro)` 支持 pro；`parse(text, pro)` 本地日期兜底；`detect` 注入真实数据 |
| `app/tools.js` | 导出 `queryToday` / `querySchedules` |
| `app/renderer/index.html` | 学科 AI modal（列表+对话+搜索）+ CSS + JS + 学习模块「问 AI」入口 + detect 去重 + aiParse 低置信度走 pro |

---

## 二、node:sqlite 在 Electron 44 实测结论（✅ 可用）

**结论：`node:sqlite`（`DatabaseSync`）在 Electron 44.0.0 主进程完全可用，无需引 sql.js，无需任何原生 npm 包。**

实测证据（Electron 44 主进程内 `require('node:sqlite')` 跑通，输出原文）：

```
TEST|module-load|PASS|node:sqlite required OK
TEST|crud|PASS|["我笔记里记的微分中值定理条件","罗尔定理 f(a)=f(b)"]
TEST|fts5-default|PASS|会议=0, 中值=0 | []
TEST|fts5-trigram|PASS|会议=0, 单字周=0
TEST|memory-db|PASS|memory db OK
TEST2|trigram-3char|PASS|次会议=0, 中值定理=1, 拉格朗日=1
TEST2|like-fallback|PASS|LIKE会议=1
TEST2|trigram-english|PASS|meeting=1
```

- `DatabaseSync` 建表 / 插入 / 查询 / 内存库（`:memory:`）全通过。
- `PRAGMA journal_mode = WAL` 生效（落盘可见 `conversations.db-shm` / `-wal`）。

### ⚠️ FTS5 中文检索的关键坑（决定检索方案）

FTS5 **默认 unicode61** 把连续中文当作单个 token，**中文短语检索完全失效**（查「会议」= 0）；**trigram** 分词最小 3 字，**中文 2 字词失效**（查「会议」「中值」= 0，查「中值定理」= 1），英文正常。

→ **最终方案：FTS5 trigram 表照建（满足 FTS5 先行路线 + 英文/长中文短语加速），实际检索走 `LIKE '%词%'` 双通道**。LIKE 对中文 100% 可靠，个人数据量（千条级）毫秒级秒回，完全满足「秒级命中」。

---

## 三、SQLite 建表 SQL 与库结构

**落点**：`{DATA_DIR}\conversations.db`（与 `workspace.json` 同目录，随快照/备份/迁移一起走，**不上 ima 云**）。

```sql
PRAGMA journal_mode = WAL;

CREATE TABLE IF NOT EXISTS conversations (
  id TEXT PRIMARY KEY,
  subject TEXT NOT NULL,        -- 学科名（隔离键）
  title TEXT,
  created_at INTEGER,
  updated_at INTEGER
);

CREATE TABLE IF NOT EXISTS messages (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  conv_id TEXT NOT NULL,        -- 关联 conversations.id
  role TEXT NOT NULL,           -- user / assistant / summary
  content TEXT NOT NULL,
  ts INTEGER
);
CREATE INDEX IF NOT EXISTS idx_messages_conv ON messages(conv_id);

-- FTS5 全文索引（trigram 分词；配合 LIKE 兜底做中文检索）
CREATE VIRTUAL TABLE IF NOT EXISTS messages_fts USING fts5(content, tokenize='trigram');
```

**隔离机制**：每学科一个 `conversations` 记录，消息经 `conv_id` 归属；对话上下文只带该 `conv_id` 的消息数组，英语会话与数学会话互不干扰（已实测：两会话 `msg_count` 分别 1/4）。

**FTS 同步**：手动双写（insert 消息同时 insert `messages_fts`），clear 时同步删除 FTS 行，避免 external-content + trigger 的兼容性风险。

---

## 四、学科档案注入（L4 雏形）

**数据源**：`data.js` 的 `rows` 中 `类型='学习'` 且 `标签∈[笔记, tut, lect]` 且 `科目===学科名`（不新开存储）。

**检索**：对提问做 2-gram 切词，对笔记 `标题+详情` 打分，取 top-5 注入 system prompt。

**注入的 system prompt 样例**（代码实构，`buildSystemPrompt` 输出）：

```
你是 Jovan 的个人工作台助手，回答简洁专业，中文优先。

以下是你的「数学」学科资料摘要（来自该学科的笔记/课件）。回答优先基于这些资料，资料没有覆盖的内容再用你的知识补充：
1. 微分中值定理
罗尔定理：闭区间连续、开区间可导、端点函数值相等，则开区间内存在一点导数为0。
```

**注入命中实测**（smoke test 原文）：`studyMaterials('数学','我笔记里记的微分中值定理条件是什么')` → 命中 `["微分中值定理"]` ✅

> ⚠️ **注入前后回答对比未能出真实样例**：DeepSeek key（尾号 9681）已失效（HTTP 401 Authentication Fails），真实 LLM 回答无法跑出。**待用户更新 key 后在 GUI 复验**（验收标准 1）。代码链路已通：`conv.sendMessage → 档案注入 → ai.chat 流式 → 持久化`，401 被正确捕获并回传 renderer。

---

## 五、窗口压缩（L1）

**触发**：单会话 `user/assistant` 消息数 **> 40 条（即 > 20 轮）** 时，发送前自动压缩。

**逻辑**：最旧 10 轮（20 条）→ 调 LLM 摘要（`ai.chatOnce` + `thinking:'off'`，200 字内）→ 删除原 20 条 → 插入 `role='summary'` 摘要消息（时间戳取被压缩区间最前，保证排序）→ 新上下文 = 摘要 + 最近 10 轮。

**上下文构建**：`summary` 消息映射为 `{role:'user', content:'（更早对话的摘要）'+摘要}`，置于历史最前。

> ⚠️ **摘要质量实测待 key 恢复**：触发条件与持久化逻辑已代码级验证，真实摘要文本质量需 key 有效后复验（验收标准 3）。费用可看：`conv:send` 返回 `usage/cost`，renderer 复用 Agent 面板的费用显示。

---

## 六、M2 四限制修复

| 限制 | 修复 | 验证 |
|---|---|---|
| 跨月日期误差 | `parse()` 日期解析改为 `normalizeDate(text) || normalizeDate(p.日期)`，本地确定性算法优先于模型换算 | 「下周三」→ `2026-09-09`（weekday=3 ✅）；「下下周一」→ `09-21`；跨月由 `new Date(y,m,d+n)` 保证 |
| 低置信度未接 pro | `pickDefaultModel(true)` 选 `deepseek-v4-pro`；`ai:parse` 加 `pro`；renderer `aiParse` 低置信度（机械解析猜类型）时传 `pro=true`，按钮文案「🤖 AI 深度解析（pro）」 | 代码级 ✅ |
| detect 冲突/逾期靠猜 | `detect()` 注入 `tools.queryToday()`（今日待办/逾期/近7天生日）真实数据到 system prompt | `queryToday` 返回 `[今日待办,逾期,近7天生日]` ✅ |
| 主动检测无防抖 | `detectIgnored`/`detectConfirmed` 去重：同一意图已确认或 60s 内已忽略 → 不重复弹；确认/忽略按钮回写状态 | 代码级 ✅ |

---

## 七、回归验证结果（全部通过）

**语法检查**：`main/conv/parse/tools/preload/data/ai.js` 全 `node --check` OK；`index.html` 内联 JS（150628 字符）`new Function` 语法检查 0 错误。

**Electron smoke test**（关键输出）：

```
SMOKE|date:下周三|PASS|2026-09-09
SMOKE|下周三=周三且未来|PASS|2026-09-09 weekday=3
SMOKE|conv-search-2char|PASS|[{"subject":"数学","content":"微分中值定理的条件"}]
SMOKE|studyMaterials|PASS|["微分中值定理"]
SMOKE|queryToday|PASS|["今日待办","逾期","近7天生日"]
```

**真实应用启动回归**：`electron .` 跑 12 秒无任何 error/exception/SyntaxError/TypeError/ReferenceError，`conversations.db` 正常创建于 `D:\Jovan's Workplace\data\`（WAL 生效）。

**真实端到端**：safeStorage 解密 key 成功（configured=true），但 `HTTP 401 key 尾号 9681 invalid` → 真实 AI 回答未出（环境问题，见遗留）。

---

## 八、坑与经验

1. **FTS5 中文分词是硬坑**：unicode61 不分中文词、trigram 最小 3 字 → 2 字中文词必须 LIKE 兜底。个人数据量 LIKE 足够，别为了「FTS5 纯血」牺牲可用性。
2. **node:sqlite 需在 Electron 主进程实测**：纯 Node 22 可用 ≠ Electron 44 可用，本 M3 已在 Electron 44 实测确认（证据见第二节）。
3. **`electron xxx.js` 跑单脚本 app name 默认「Electron」** → safeStorage 读不到真实 key；测试脚本须 `app.setName("Jovan's Workplace")`（productName）在 `whenReady` 前。
4. **Electron renderer 不支持 `window.prompt/confirm`** → 学科 AI 的「新建/重命名/清空」改用内联输入行 + 二次点击确认，避免静默失效。
5. **主色沿用现有 `--orange` 变量（实为青绿 #16856f）**，不引入 InspectFlow 的橙白灰（那是另一项目）。

---

## 九、遗留事项（待用户 / 待复验）

| 项 | 说明 | 阻塞原因 |
|---|---|---|
| 验收 1/3 真实 AI 回答 | 档案注入效果、25+ 轮压缩摘要质量 | **DeepSeek key 尾号 9681 已失效（401）**，需用户更新 key 后复验 |
| 验收 4-6 交互复验 | 搜索跳转、跨月、detect 去重在 GUI 的观感 | 需用户实际点按 |
| 语义搜索（sqlite-vec） | 蓝图进阶项 | M4+（本 M3 明确不做） |

**更新 key 后复验路径**：设置 → AI 模型 → DeepSeek 重新粘贴有效 key → 学习模块选「数学」科目 → 点「🤖 问 AI」→ 问「我笔记里记的微分中值定理条件是什么」→ 应引用笔记内容。

---

## 十、commit 建议

```
feat(m3): 学科 AI 会话管理器 + FTS5 历史搜索 + 档案注入 + M2 限制修复
```

（实际提交/推送由下一动作执行）
